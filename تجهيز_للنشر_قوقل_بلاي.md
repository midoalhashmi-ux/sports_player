# تعليمات تجهيز تطبيق sports_player للنشر على Google Play

هذه رسالة جاهزة تعطيها لأي نموذج ذكاء اصطناعي يعمل على مستودع `sports_player`.
الصق معها ملف `project-protection-rules.md` كما هو دائماً قبل البدء.

---

## قبل ما تبدأ

اقرأ `project-protection-rules.md` والتزم به حرفياً. البنود أدناه مقسّمة لثلاث فئات:

- **[تنفيذ مباشر]** — ملفات غير محمية، نفّذها مباشرة وسلّم الملفات المعدّلة/الجديدة كأرشيف ZIP.
- **[يحتاج إذن صريح]** — يمس ملف من القائمة المحمية (`codemagic.yaml`, `build.gradle`, إلخ). **توقف واشرح التعديل بالضبط وانتظر "نعم" صريحة** قبل التنفيذ، حتى لو الرسالة هذي تطلبه — لأن الإذن يجب أن يصدر مني مباشرة وقت التنفيذ الفعلي، لا مسبقاً.
- **[خطوة يدوية]** — خارج الكود بالكامل (حساب Firebase، Play Console، AdMob)، أنا أنفذها بنفسي.

---

## طريقة التعامل مع البنود المُعلَّمة [خطوة يدوية]

أنا **لا أفهم برمجة إطلاقاً**. أي بند مُعلَّم [خطوة يدوية] بالأسفل — بدل ما تشرحه لي دفعة وحدة أو تفترض إني أعرف أنفّذه، اتبع هذا الأسلوب بالضبط:

1. اشرح لي أولاً بجملة أو جملتين بسيطة **ليش** هذي الخطوة لازمة (بدون مصطلحات تقنية).
2. بعدها وجّهني **خطوة خطوة**: شنو أفتح بالضبط (اسم الموقع/التطبيق)، وين أضغط بالضبط (اسم الزر أو القائمة كما يظهر لي حرفياً على الشاشة)، وشنو أكتب لو يحتاج إدخال نص.
3. أعطني **خطوة واحدة فقط** بكل رسالة، واستنى ردّي (أو وصف لقطة شاشة مني) قبل ما تعطيني الخطوة التالية — لا تعطيني قائمة طويلة دفعة وحدة.
4. لو طلعت لي رسالة خطأ أو شاشة غير متوقعة، اطلب مني أوصفها لك بالضبط (أو أرفق لقطة شاشة) قبل ما تقول لي شنو أسوي بعدها.
5. بعد ما أخلّص كل خطوة يدوية وتترتب عليها قيمة (مثل رابط، معرّف، أو كود)، اطلب مني أعطيك هذي القيمة بالضبط حتى تكمل بيها أي جزء من الكود يحتاجها (مثل معرّف AdMob في البند 2).
6. لا تفترض أبداً إني أعرف أسماء مثل "Keystore" أو "SHA-1" أو "Firestore" — أول ما تجيك، عرّفها بجملة وحدة بسيطة قبل ما تكمل.

---

## 🔴 حرجة

### 1) Network Security Config — يسمح لروابط HTTP بالعمل [تنفيذ مباشر]

المشكلة: أندرويد 9+ يمنع أي اتصال HTTP غير مشفّر افتراضياً. بعد فتح ميزة "إضافة رابط" لأي مستخدم، أي رابط بث HTTP (شائع بروابط IPTV) سيفشل بصمت.

الحل:

**ملف جديد** `android/app/src/main/res/xml/network_security_config.xml`:
```xml
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
```

**تعديل** `android/app/src/main/AndroidManifest.xml` — أضف السطر التالي داخل وسم `<application ...>` (بجانب `android:icon`):
```xml
android:networkSecurityConfig="@xml/network_security_config"
```

### 2) استبدال معرّف تطبيق AdMob بمعرّف حقيقي [تنفيذ مباشر للكود / خطوة يدوية للحصول على المعرّف]

الموجود حالياً معرّف اختبار Google (`ca-app-pub-3940256099942544~3347511713`) في:
- `android/app/src/main/AndroidManifest.xml` (وسم `com.google.android.gms.ads.APPLICATION_ID`)
- `lib/services/ad_service.dart` (المتغيّر `_testAppId` — يبقى كما هو، هذا مرجع للمطابقة فقط ولا يُغيَّر)

**[خطوة يدوية]** أحصل بنفسي على App ID الحقيقي من حساب AdMob (Apps → App settings → App ID).

**[تنفيذ مباشر]** بعد ما أعطيك المعرّف: استبدله في `AndroidManifest.xml` فقط، ولا تلمس `_testAppId` في `ad_service.dart`. ثم أدخل نفس المعرّف بالضبط في حقل "معرّف التطبيق" بلوحة التحكم (تبويب الإعلانات) حتى تُفعَّل أكواد الوحدات الإعلانية الحقيقية تلقائياً (المنطق موجود مسبقاً بـ `ad_service.dart`، لا يحتاج تعديل).

### 3) بصمة SHA-1 لمفتاح التوقيع الفعلي [خطوة يدوية]

أنفّذها بنفسي: أولّد/أستخدم keystore التوقيع الحقيقي (release، ليس debug)، أستخرج بصمة SHA-1 منه، وأضيفها في Firebase Console → Project settings → التطبيق بحزمة `com.midoalhashmi.zoltrastream` → Add fingerprint. بدونها قد تفشل نداءات Cloud Function للقنوات المحمية بالنسخة الموقّعة للنشر تحديداً.

### 4) بناء App Bundle بدل APK [يحتاج إذن صريح — يمس `codemagic.yaml`]

اطلب إذني صراحة، ثم عدّل خطوة البناء في `codemagic.yaml` من:
```
flutter build apk --release
```
إلى:
```
flutter build appbundle --release
```
وحدّث سطر `artifacts:` ليطابق مخرجات `.aab` بدل `.apk`. اشرح لي التغيير بالضبط وانتظر موافقتي قبل التسليم.

### 5) سياسة خصوصية مستضافة (رابط عام) [خطوة يدوية]

Play Console يطلب رابط ويب فعلي (ليس شاشة داخل التطبيق) في حقل "Privacy policy". أنفّذها بنفسي — ممكن أطلب من نموذج آخر يبني صفحة HTML بسيطة أستضيفها (GitHub Pages مثلاً)، لكن هذا خارج نطاق مستودع `sports_player` نفسه.

---

## 🟠 مهمة

### 6) تقوية نص الشروط بخصوص مسؤولية المستخدم عن الروابط [تنفيذ مباشر]

عدّل `_fallbackTerms` في `lib/screens/terms_privacy_screen.dart` ليذكر صراحة: المستخدم وحده مسؤول عن أي رابط بث يضيفه، ويجب أن يملك الحق القانوني لبثه/مشاهدته، والتطبيق لا يستضيف أو يوفّر أي محتوى بنفسه. لا تغيّر بنية الشاشة، فقط النص الاحتياطي. (نفس النص لازم يُحدَّث لاحقاً بحقل `settings/legal.terms` بلوحة التحكم — هذا خارج مستودع المشغل).

### 7) نموذج Data Safety بـ Play Console يطابق الواقع [خطوة يدوية]

أنفّذها بنفسي وقت تعبئة النموذج بالكونسول، بناءً على: (أ) رسائل "اتصل بنا" (اسم + رسالة) تُرسَل لـ Firestore، (ب) بيانات إعلانات AdMob عبر UMP (معرّف إعلاني). لا حاجة لتعديل كود لهذا البند.

### 8) Adaptive Icon (foreground/background منفصلين) [تنفيذ مباشر — إن توفر ملف الأيقونة بطبقتين]

لو أرسلت لك نسخة من الأيقونة بخلفية شفافة (foreground فقط)، أنشئ:
- `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`
- `android/app/src/main/res/values/ic_launcher_background.xml` (لون الخلفية)
- ملفات `ic_launcher_foreground.png` بكل مجلدات `mipmap-*` الموجودة أصلاً.

لا تحذف ملفات `mipmap-*/ic_launcher.png` الحالية (تبقى fallback للأجهزة الأقدم من أندرويد 8).

### 9) قواعد ProGuard لتفادي كراشات ما بعد التصغير [تنفيذ مباشر]

أنشئ/حدّث `android/app/proguard-rules.pro` بقواعد `-keep` لحزم: `firebase_core`, `cloud_firestore`, `google_mobile_ads`, `video_player`. اربطه بـ `getDefaultProguardFile('proguard-android-optimize.txt')` الموجود أصلاً بـ `android/app/build.gradle` — **لا تعدّل `build.gradle` نفسه**، فقط أضف سطر `proguardFiles` الإضافي لو لم يكن موجوداً (وهذا يُعتبر تعديلاً على ملف محمي، فاطلب إذني أولاً أيضاً قبل لمس `build.gradle`).

### 10) التحقق من targetSdkVersion وقت البناء الفعلي [خطوة يدوية]

أتحقق بنفسي وقت البناء عبر Codemagic (Flutter 3.27.0) أن `targetSdkVersion` الناتج ≥ 34. لا حاجة لتعديل كود.

---

## 🟡 تحسينات هوية (اختيارية، غير حرجة)

### 11) توحيد اسم التطبيق بكل مكان [تنفيذ مباشر]

وحّد الاسم الظاهر للمستخدم إلى اسم واحد (مثلاً "SH Player") في:
- `lib/main.dart` (`title:` و`AppBar`/الشاشات التي تعرض "BinSheikh Player")
- `android/app/src/main/AndroidManifest.xml` (`android:label`)
- `lib/screens/home_screen.dart` (نص "BinSheikh Player" بالـ AppBar والـ Drawer)

لا تغيّر `applicationId`/`namespace` إطلاقاً (محمي، ومحدَّد بالفعل).

### 12) حذف أو تفعيل splash_screen.dart [تنفيذ مباشر بعد قراري]

`lib/screens/splash_screen.dart` كود غير مستخدم فعلياً (main.dart يتجاوزه مباشرة لـ HomeScreen). اسألني: أحذفه، أو أفعّله كشاشة بداية فعلية بنفس اسم العلامة الموحّد من البند 11؟

### 13) تحديث تعليق قديم بـ pubspec.yaml [تنفيذ مباشر]

عدّل تعليق `video_player` في `pubspec.yaml` (الذي يذكر DASH/RTMP/MKV) ليعكس الواقع: يدعم HLS وMP4/التقدمي فقط عبر ExoPlayer على أندرويد. تعليق فقط، لا تغيير بالحزمة نفسها (ممنوع أصلاً حسب قواعد الحماية).

---

## ترتيب التنفيذ المقترح

1. البند 1 (Network Security Config) — تنفيذ فوري، بدون أي إذن مطلوب.
2. البند 6، 9، 11، 12، 13 — تنفيذ مباشر بعد ردّي على أسئلتها البسيطة.
3. البند 2 — بعد ما أعطيك معرّف AdMob الحقيقي.
4. البند 4 و9(جزء build.gradle) — تطلب إذني صراحة قبل التنفيذ.
5. البنود 3، 5، 7، 10 — أنفّذها بنفسي خارج الكود بالتوازي.

سلّم كل مجموعة كأرشيف ZIP منفصل بمساراتها الصحيحة، مثل باقي المشروع.
