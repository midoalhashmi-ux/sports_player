# حالة تجهيز sports_player للنشر — آخر تحديث: 2026-09-01

هذا الملف مرجع تتبّع لأي نموذج ذكاء اصطناعي يكمل العمل على هذا المشروع.
اقرأه دائماً أولاً قبل البدء بأي بند، وحدّثه بعد كل خطوة تنجز.

الرموز: ✅ منجز | ⏳ معلّق (ينتظر شيء) | ⏸️ متوقف مؤقتاً | ❓ غير معروف الوضع

---

## 🔴 حرجة

1. **Network Security Config (HTTP)** — ✅ منجز. الملف موجود ومربوط بـ AndroidManifest.xml.
2. **معرّف AdMob الحقيقي** — ⏳ معلّق. صاحب المشروع أوقف الإعلانات مؤقتاً لحين تفعيل الحساب وإعطاء المعرّف الحقيقي.
3. **بصمة SHA-1** — ⏸️ متوقف مؤقتاً. الـ keystore موجود على Codemagic (اسم المرجع: `sports_player_release`) لكن الملف الفعلي غير محفوظ محلياً عند صاحب المشروع. أُضيفت خطوة مؤقتة بـ `codemagic.yaml` تطبع SHA-1 أثناء البناء (script باسم "طباعة بصمة SHA-1") — **البناء رقم #47 عالق بحالة queued** بسبب زحمة أجهزة البناء المجانية على Codemagic (تأكدنا: الرصيد سليم 18/500 دقيقة، المشكلة طابور انتظار). صاحب المشروع قرر ينتظر أو يفعّل اشتراك مدفوع لاحقاً. **مهم جداً: بعد الحصول على البصمة، يجب حذف خطوة "طباعة بصمة SHA-1" من codemagic.yaml وإرجاعه لوضعه الأصلي.**
4. **App Bundle بدل APK** — ⏳ معلّق. يحتاج إذن صريح (يمس codemagic.yaml).
5. **سياسة خصوصية مستضافة (رابط عام)** — ❓ قيد العمل الآن (الخطوة الحالية).

## 🟠 مهمة

6. **تقوية نص الشروط** — ⏳ معلّق، ينتظر تأكيد/تعديل الصياغة من صاحب المشروع.
7. **نموذج Data Safety بـ Play Console** — ❓ غير معروف الوضع.
8. **Adaptive Icon** — ⏳ معلّق، ينتظر ملف أيقونة بطبقتين (foreground شفاف) من صاحب المشروع.
9. **قواعد ProGuard** — ⚠️ منفّذ جزئياً. ملف `proguard-rules.pro` موجود وفيه قواعد صحيحة، لكنه **غير مربوط فعلياً** بـ `android/app/build.gradle` (لا يوجد سطر proguardFiles إضافي يشير له). يحتاج إذن صريح لتعديل build.gradle.
10. **التحقق من targetSdkVersion وقت البناء** — ❓ غير معروف الوضع، يدوي بالكامل.

## 🟡 تحسينات هوية

11. **توحيد اسم التطبيق** — ⏳ معلّق، ينتظر قرار صاحب المشروع بالاسم النهائي (لسا "BinSheikh Player" بكل مكان).
12. **حذف/تفعيل splash_screen.dart** — ⏳ معلّق، ينتظر قرار صاحب المشروع.
13. **تعليق pubspec.yaml عن video_player** — ✅ منجز.

---

## ملاحظات مهمة لأي نموذج يكمل العمل

- المشروع مرتبط بحساب Codemagic لصاحب المشروع (تطبيق `sports_player`، فرع `main`).
- keystore التوقيع مرفوع على Codemagic باسم مرجعي `sports_player_release` — لا تطلب من صاحب المشروع رفع ملف .jks جديد بدون داعٍ، هو غير محفوظ عنده محلياً.
- صاحب المشروع **لا يفهم برمجة إطلاقاً** — أي بند [خطوة يدوية] يُشرح بجملة بسيطة عن السبب، ثم خطوة واحدة بكل رسالة، وينتظر رده أو وصف/صورة قبل الخطوة التالية.
- التزم بـ `project-protection-rules.md` حرفياً قبل أي تعديل.
