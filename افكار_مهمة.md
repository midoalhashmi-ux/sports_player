# أفكار مهمة — خطط مستقبلية لاستقرار وسرعة التشغيل

> ملف مرجعي لأفكار نوقشت بعمق (مو تنفيذ فعلي بعد، إلا ما ذُكر صراحة إنه
> منفَّذ). الهدف: أي جلسة قادمة (أنا أو نموذج ثاني) تكمل من هنا بدل ما
> تعيد نفس النقاش والتشخيص من الصفر.

## السياق: المشكلة الأصلية

استقرار تشغيل الفيديوهات المستوردة من مواقع خارجية (ristoanime.me،
topcinema.fan، وغيرها — مو مصادركم أنتم) يعتمد بالكامل على استقرار
الموقع المصدر نفسه وقت التشغيل. الهدف: تقليل هذا الاعتماد قدر الإمكان.

---

## 1) ✅ مشكلة انقطاع الاتصال وقت جلب الشرائح — محلولة فعلياً

**التشخيص**: سجلات تشخيص حقيقية أظهرت أخطاء متكررة (`Connection closed
while receiving data`) عند جلب شرائح HLS، بفارق أقل من 5 ميلي ثانية بين
كل محاولة — نمط كلاسيكي لخادم يحمي نفسه من عدد اتصالات متزامنة كبير من
نفس العميل، **لا علاقة له بجودة الشبكة فعلياً**.

**الحل المنفَّذ** (بالكود فعلاً، `lib/services/hls_cache_proxy.dart`):
حد أقصى `_maxConcurrentPrefetch = 3` اتصالات فعلية بنفس اللحظة، مع طابور
انتظار للباقي، بدل إطلاق كل شرائح الهامش دفعة واحدة.

**لا حاجة لأي عمل إضافي هنا** إلا لو ظهر نمط فشل جديد مختلف بسجلات
تشخيص مستقبلية.

---

## 2) فكرة "سجل المحلِّلات" الأصلية — غير قابلة للتطبيق كما صُمِّمت

**الفكرة الأصلية**: بدل استخراج رابط الفيديو النهائي بالكامل داخل
WebView (بطيء، ~6-8 ثوانٍ أولية)، يكتشف الجهاز فقط رابط صفحة الـembed،
وطلب HTTP خفيف (بدون متصفح) يحلّها لرابط الفيديو النهائي بسرعة.

**السبب الحقيقي وراء رفضها**: تحقّقنا فعلياً (طلب HTTP مباشر لصفحة
embed حقيقية من موقع `vidtube.one`) ولقينا **تحدي Cloudflare JS حقيقي**
("Just a moment...") يحظر أي طلب مو من متصفح حقيقي **قبل** الوصول لأي
محتوى فعلي. لا يوجد أي تحايل هيدر أو تعمية يتجاوز هذا — يحتاج محرّك
جافاسكريبت حقيقي (بالضبط زي WebView الحالي) ليحل التحدي أولاً.

**الخلاصة**: أي "محلِّل خفيف بدون متصفح" مستحيل تقنياً لأي مزوّد محمي
بهذا الأسلوب. هذا يفسّر لماذا المعمارية الحالية تعتمد WebView أصلاً —
قرار صحيح، مو نقص تحسين.

---

## 3) 🔜 البديل المقترح: "وصفة موقع" تتعلّم ذاتياً (لم يُنفَّذ بعد)

بدل استبدال WebView، نخليه **أسرع بدل أبطأ** لمواقع رآها التطبيق من
قبل ونجح معها:

- **التشخيص**: حلقة الاكتشاف تفحص كل **2 ثانية بالضبط**
  (`Timer.periodic(const Duration(seconds: 2), ...)` بـ
  `watch_screen_discovery.dart`)، وتحتاج غالباً دورتين (~4 ثوانٍ) قبل ما
  تثق بمرشّح iframe وترقّيه — هذا مصدر التأخير الأساسي (~6.4 ثانية
  بسجل حقيقي).
- **الفكرة**: أول نجاح لدومين مصدر معيّن (مثلاً `topcinema.fan`) يُسجَّل
  محلياً (`shared_preferences`، أداة مستخدَمة أصلاً بالمشروع): أي نمط
  المرشّح الناجح (crossHost مثلاً)، ونوع اللاعب المكتشَف. أي زيارة تالية
  لنفس الدومين تُرقّي أول مرشّح مطابق **فوراً** بدل انتظار دورة تأكيد
  ثانية، وتتخطى محاولات "التنشيط" (priming) المعروف أصلاً إنها غير
  ضرورية لهذا النمط.
- **حماية ذاتية**: فشل الوصفة المحفوظة (الموقع غيّر بنيته) = رجوع فوري
  للمسار الكامل العادي + تحديث الوصفة بنتيجة جديدة.
- **لماذا هذا أفضل من الفكرة الأصلية**: يشتغل بالكامل جوا WebView
  (يحترم قيد Cloudflare)، بدون أي سيرفر/ووركر جديد، ويتعلّم تلقائياً من
  الاستخدام الفعلي بدل سجل يدوي يحتاج صيانة مستمرة لكل مزوّد جديد.

**الخطوة التالية لو تنفَّذت**: تصميم شكل "الوصفة" المخزَّنة + نقطة
التدخّل بالضبط داخل حلقة الاكتشاف الحالية.

---

## 4) لماذا "الووركر ينتحل جلسة WebView" غير عملي (لمواقع Cloudflare تحديداً)

فكرة نُوقشت: الووركر ياخذ كوكيز/معلومات جلسة WebView بعد ما تحل تحدي
Cloudflare بنجاح، ويستخدمها هو نفسه ليطلب من نفس المصدر.

**السبب التقني وراء الرفض**: كوكي `cf_clearance` (اللي يصدره Cloudflare
بعد حل التحدي بنجاح) **مربوط بهوية الطالب الأصلي** (IP + بصمة
المتصفح/TLS)، مو تذكرة عامة. نقله لسيرفر مختلف (الووركر، IP وبصمة
مختلفتين تماماً) يُكتشَف ويُرفَض من Cloudflare نفسه — هذا بالضبط الغرض
من وجود هذي الحماية: منع "حلّ التحدي بمكان، استخدام النتيجة بمكان ثانٍ".

**قد تنجح فقط** لمزوّدين بحماية أخف (فحص Referer/كوكي عادي بدون ربط
IP/بصمة) — لا نعرف بعد إن كان يوجد مثل هذا بمصادركم الفعلية، يحتاج
لوقات تشخيص من مزوّدين إضافيين للتأكد.

---

## 5) 💡 الفكرة الأقوى: كاش شرائح مشترك (لم يُنفَّذ، تحتاج قرار عمل قبل تقني)

**الملاحظة الأساسية**: نفس الحلقة يشاهدها مئات المستخدمين المختلفين —
المحتوى نفسه لكل واحد، مو شخصي. فليش كل واحد يواجه Cloudflare من الصفر؟

**الآلية المقترحة**:
1. أول مستخدم يفتح حلقة → جهازه (WebView) يحل التحدي بهويته الحقيقية
   ويشغّل الفيديو محلياً (زي الآن بالضبط، بدون أي تغيير هنا).
2. **بالتوازي**، الجهاز يرسل نسخة من الشرائح الفعلية اللي جلبها بنجاح
   للووركر — تخزين فقط، **مو إعادة طلب بهوية مزيّفة** (يتجنّب مشكلة
   القسم 4 كلياً لأنه ما فيه أي انتحال هوية).
3. الووركر يخزّنها (Cloudflare R2) **مربوطة بالحلقة نفسها**، مو بجلسة
   أي مستخدم.
4. أي مستخدم ثانٍ يفتح نفس الحلقة لاحقاً → يستلمها مباشرة من الكاش —
   بدون أي اتصال بالموقع المصدر أو Cloudflare إطلاقاً.

**حدود Cloudflare الفعلية (تحقّقتها)**:
- Workers KV: أقصى 25 ميجا لكل قيمة — غير مناسب لشرائح فيديو.
- **R2 أنسب**: خطة مجانية 10 جيجا تخزين شهرياً + **بدون رسوم سحب
  بيانات (egress)**، بدون حد صارم على حجم الملف الواحد عملياً.
- 10 جيجا تُستهلَك بسرعة لمكتبة كاملة — يحتاج سياسة إخلاء، مو تخزين
  دائم.

**سياسة مقترحة (لازمة، مو اختيارية)**:
- **صلاحية زمنية (TTL)**: 30-60 يوم من آخر طلب — يُحذف تلقائياً لو ما
  طُلب خلالها (Cron بالووركر، نفس نمط "مزامنة المباريات كل ساعة"
  الموجود أصلاً بالمشروع).
- **حد أقصى لحجم التخزين الكلي** + إخلاء الأقدم استخداماً (LRU) أول لو
  اقترب من الحد.
- ابدأ بحد متواضع (5-8 جيجا) تحت الحد المجاني، راقب الاستخدام الفعلي
  قبل أي التزام بميزانية أكبر.

**⚠️ قرار عمل/قانوني، مو تقني بحت**: هذا يخلي البنية التحتية **تحتفظ
بنسخ فعلية من محتوى طرف ثالث** (ولو مؤقتاً ومتجدداً) — التزام أكبر من
الوضع الحالي (كل جهاز يجيب المحتوى لحاله لحظياً بدون أي تخزين مركزي).
كاش بصلاحية محدودة ومتجدد تلقائياً أقرب لسلوك CDN عادي من "أرشيف دائم"،
لكنه يبقى قراراً يستاهل وعياً كاملاً قبل التنفيذ، مو بس قرار هندسي.

---

## ترتيب أولوية مقترح لو رجعنا لهذا الملف لاحقاً

1. **جمع لوقات تشخيص من مزوّدين/مواقع إضافيين** (غير `vidtube.one`) —
   كل الأفكار أعلاه (خصوصاً #3 و#4) تحتاج بيانات حقيقية أكثر قبل تعميم
   أي قرار تصميم عليها.
2. تنفيذ **#3** (وصفة الموقع المتعلّمة) — أثر مباشر على سرعة الاستخراج،
   بدون أي قرار عمل/قانوني معلَّق، وبدون أي بنية سيرفر جديدة.
3. اتخاذ قرار واعٍ بخصوص **#5** (الكاش المشترك) — الأثر الأكبر على
   الاستقرار العام، لكن يحتاج نقاش عمل/قانوني أول قبل أي تنفيذ.
