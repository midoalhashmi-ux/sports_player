# التوثيق التقني — sports_player (تطبيق المشغل)

> هذا الملف مرجع تقني شامل لهذا المستودع، بحيث يقدر أي مبرمج أو نموذج ذكاء
> اصطناعي آخر يفهم بنية المشروع كاملة بدون الحاجة لقراءة كل الكود من الصفر.
> **كل تعديل جوهري أو إصلاح مشكلة لاحق يُضاف كصف جديد في جدول "سجل المشاكل
> والحلول" بالأسفل** — سطر واحد مختصر لكل عمود، بدون كود أو شرح مطوّل
> (التفاصيل الكاملة موجودة برسالة الكوميت نفسها، هذا الجدول فهرس سريع فقط).

## 1. نظرة عامة على المنظومة الكاملة (3 مستودعات مترابطة)

هذا المشروع جزء من منظومة من 3 مستودعات منفصلة على GitHub تتشارك نفس
مشروع Firebase ونفس Cloudflare Worker:

| المستودع | الدور |
|---|---|
| **BinSheikh** | تطبيق المحتوى الرئيسي (Flutter/Android) — يعرض للمستخدم النهائي الأقسام/القنوات/الأفلام/المسلسلات/الأنمي/مباريات اليوم. |
| **sports_player** (هذا المستودع) | تطبيق Flutter/Android منفصل، وظيفته الوحيدة تشغيل الفيديو الفعلي. يُفتح من BinSheikh عبر `app_links` بمعرّف قناة/حلقة. فصله كتطبيق مستقل يتيح تحديثه في Play Store بدون أي حاجة لتحديث BinSheikh، ويعزل منطق حل الروابط الحقيقية/الحماية عن تطبيق المحتوى العام. |
| **AHMED-dashboard** | لوحة تحكم ويب ثابتة (HTML/JS/CSS خام، بدون أي أداة بناء) يديرها فريق المحتوى — إضافة/تعديل الأقسام والقنوات والروابط، ومتابعة إحصائيات المشاهدة. |

- **مشروع Firebase المشترك**: `sports-stream-app-36a7a` (نفس Firestore، نفس
  Auth) — راجع `lib/firebase_options.dart`.
- **Cloudflare Worker المشترك**: `https://binsheikh-api.binsheikh.workers.dev`
  (كوده المصدري داخل مستودع BinSheikh في `cloudflare-worker/`) — يخدم روابط
  البث الحقيقية ومباريات اليوم والتقييمات واستيراد المواقع.

## 2. هذا المستودع (sports_player)

- **الغرض**: تطبيق Flutter/Android، وظيفته الوحيدة تشغيل فيديو HLS/MP4 لقناة
  أو حلقة معيّنة يُمرَّر إليه معرّفها.
- **Flutter SDK المستخدم**: 3.27.0 (حسب `codemagic.yaml`).
- **التبعيات الأساسية** (`pubspec.yaml`):
  - `video_player` — التشغيل الأصلي (ExoPlayer على أندرويد)، يدعم HLS وMP4
    فقط عملياً (لا DASH/RTMP/MKV رغم ما كان مكتوباً سابقاً بالكود).
  - `webview_flutter` — لعرض صفحات بث كويب عند `streamType=web`.
  - `firebase_core` + `cloud_firestore`.
  - `http` — لطلبات StreamAuthService والتشخيص.
  - `wakelock_plus`, `google_mobile_ads`, `share_plus`, `url_launcher`,
    `package_info_plus`, `shared_preferences`, `image_gallery_saver_plus`.

### بنية `lib/`

- `main.dart` — نقطة الدخول: تهيئة Firebase + معالج أخطاء عام
  (`runZonedGuarded` + `FlutterError.onError` + `PlatformDispatcher.onError`)
  يسجّل أي خطأ غير ملتقط عبر `SessionLogService`.
- `screens/watch_screen.dart` (~4700 سطر) — **الشاشة الأهم والأكبر**. آلة
  حالة كاملة لتشغيل الفيديو: تجربة عدة سيرفرات/جودات (`_WebNetworkCandidate`)،
  التبديل بين تشغيل native (`video_player`) وWebView، اكتشاف تلقائي لمصدر
  الفيديو من صفحة ويب (`_autoDetectWebSource`) عبر تتبّع طلبات شبكة داخل
  WebView، وتسجيل أحداث تشخيصية عبر `SessionLogService` (`_slog`) لتحليلها
  لاحقاً من سجل مُصدَّر (راجع مهارة `stream-debug` إن وُجدت في
  `.claude/skills/`).
- `screens/home_screen.dart`, `add_url_screen.dart`, `contact_screen.dart`,
  `force_update_screen.dart`, `terms_privacy_screen.dart` — شاشات مساعدة.
- `services/stream_auth_service.dart` — يطلب جلسة بث من الووركر
  (`POST /getStreamUrl`)، يدعم شكل استجابة متعدد السيرفرات/الجودات وشكل قديم
  مبسّط (`{url, expiresIn}`) لتوافق رجعي.
- `services/channel_source_resolver.dart`, `api_source_resolver.dart` — حل
  مصدر القناة (رابط مباشر مخزَّن أو مصدر API ديناميكي).
- `services/session_log_service.dart` — يجمع سجلاً نصياً زمنياً لكل جلسة
  تشغيل، قابلاً للتصدير — هذا هو أساس التشخيص عن بعد بدون وصول فعلي لجهاز
  المستخدم.
- `services/native_cookie_service.dart`, `worker_config.dart`,
  `version_check_service.dart`, `feature_flags_service.dart`,
  `pip_service.dart`, `player_visibility_service.dart`,
  `saved_link_service.dart`, `ad_service.dart`.
- `theme/app_theme.dart` — خط Tajawal + الألوان الموحّدة.

## 3. تدفق تشغيل الفيديو (مختصر)

1. يُفتح `watch_screen` بمعرّف قناة/حلقة (`channelId`) من BinSheikh عبر
   `app_links`، أو يدوياً من `add_url_screen`.
2. `StreamAuthService.requestSession(channelId)` → `POST /getStreamUrl` على
   الووركر.
3. الووركر يتحقق من Firestore (`channels` + `privateStreams`) ويرجّع إما
   رابطاً واحداً أو عدة سيرفرات/جودات.
4. `watch_screen` يجرّب كل مصدر بالترتيب: تشغيل native أولاً
   (`video_player`/ExoPlayer)، ولو فشل أو كان `streamType=web` يحوّل لـ
   WebView مع اكتشاف تلقائي للمصدر الحقيقي من طلبات الشبكة.
5. كل خطوة تُسجَّل عبر `SessionLogService` — هذا هو السجل الذي يُصدَّر من
   التطبيق ويُستخدم للتشخيص.

## 4. الحماية الأمنية

روابط m3u8 الحقيقية **غير مخزّنة إطلاقاً** بهذا التطبيق ولا تُقرأ مباشرة من
Firestore — تُجلب فقط عبر `/getStreamUrl` بالووركر (الذي يقرأ
`privateStreams` بصلاحيات خاصة لا تصل لأي عميل).

## سجل المشاكل والحلول

| # | المشكلة | الحل | الكوميت |
|---|---|---|---|
| 1 | رابط بمسار مزدوج `//` (`vidtube.cam//`) لا يُكتشف كـ"غير وسائط" | تعميم `_isNonMediaAsset` ليطابق أي عدد شرطات، لا `/` فقط | `995a558` |
| 2 | حالة `nativePlaying` تُستبدل بصمت بعد نجاح التشغيل (يعيد اكتشاف الويب) | حارس مركزي داخل `_setWebSessionState` يرفض مغادرتها | `d6d1d28` |
| 3 | كود ميت + تحذيرات lint (3 ملفات غير مستخدمة، حقول ميتة...) | حذف/تنظيف + `analysis_options.yaml` (`flutter_lints`) | `3c9e51c` |
| 4 | لا معالج أخطاء عام — خطأ غير ملتقط يُسقط التطبيق بصمت | `runZonedGuarded`+`FlutterError.onError`+`PlatformDispatcher.onError` بـ`main.dart` | نفس `3c9e51c` |
| 5 | تعذّر تشخيص "Source error" من ExoPlayer بالسجل النصي وحده | تسجيل أحداث buffering/error + فحص HTTP Range تشخيصي عند فشل native | `1eae933`, `721cde3` |
| 6 | إخفاء صفحة المصدر (إعداد إلزامي) أقل موثوقية من إظهارها لمواقع JWPlayer/Video.js — Chromium لا يحتسب نقرة JS محاكاة كنقرة حقيقية لـautoplay | كشف مشغّلات عام بالخاصية (لا بالدومين) + كتم صوت أثناء التحضير بالإخفاء + رفض ادّعاء "جاهز" بدون دليل تشغيل حقيقي | `4b7cda6` |
| 7 | `_normalizeCandidate` يضيف `#` زائدة لكل رابط مصدر (حتى بلا fragment أصلاً) — يفسد مطابقة سجل المرشحين/منع التكرار | `uri.replace(fragment:'')` ↔ `uri.removeFragment()` (مؤكَّد باختبار فعلي على SDK 3.27.0) | `1387fc1` |
| 8 | فشل متكرر `ExoPlaybackException: Source error` لمصادر HLS حقيقية تعمل بنجاح داخل WebView (مؤكَّد بسجلَين حقيقيَين — grzcdn.com) — السبب: رابط المرشّح يصل لمرحلة التشغيل الأصلي أحياناً بـ`/` إضافية لم تكن موجودة وقت تسجيله، فيفشل بحث `_webCandidateRegistry` ويسقط `formatHintOverride` لـ`null`، فيحاول ExoPlayer تخمين نوع الملف من امتداد الرابط ويفشل لأنه لا ينتهي بـ`.m3u8` حرفياً | تمرير تلميح HLS صريح كلما كان `strongHls` (نمط الرابط، مثل مقطع `/hls/`) يثق بالمصدر أصلاً — كان محسوباً مسبقاً بنفس الدالة ولم يُستخدم هنا | `dfcf47f` |
| 9 | زر وضع العرض محدود بـ3 خيارات فقط — لا يكفي كل نِسب الشاشات | إضافة `fitWidth`/`fitHeight` (5 خيارات الآن) | `de0aaa4` |
| 10 | `didChangeAppLifecycleState` يتجاهل تشغيل WebView تماماً (شائع جداً — كل مواقع JWPlayer/Video.js المعروفة) — عند تصغير التطبيق يستمر صوت/فيديو الصفحة وتنقّلها الإعلاني بالخلفية بصمت | إيقاف/استئناف عناصر `<video>/<audio>` بالـWebView بنفس منطق المشغّل الأصلي | `de0aaa4` |
| 11 | تخزين مؤقت (buffering) بلا تقدّم أثناء المشاهدة الفعلية قد يستمر للأبد بدون أي مخرج للمستخدم غير الخروج يدوياً | مراقب: محاولة إنعاش (seekTo) بعد 20 ثانية، وإذا فشلت الانتقال لشاشة الخطأ الموجودة (بزر إعادة المحاولة) بعد 20 ثانية إضافية | `de0aaa4` |
| 12 | قائمة وضع العرض المنسدلة تُخفي خياراتها السفلية خارج الشاشة على بعض الأجهزة (بلا إمكانية سحب لأعلى) | تبديل بنقرة واحدة (تدوير على الأوضاع) بدل قائمة، مع شارة نصية مؤقتة وسط الشاشة باسم الوضع | `b0262a9` |
| 13 | لا تكبير/تصغير بإصبعين (Pinch-to-zoom) بمشغّل الفيديو | أُضيف (100%=الحجم الطبيعي، شارة نسبة مؤقتة) — تطلّب توحيد إيماءات التقديم/الصوت الحالية مع onScale* لأن Flutter يمنع خلطها بـonHorizontalDrag*/onVerticalDrag* على نفس الأداة | `b0262a9` |
| 14 | تقطيع خفيف متكرر أثناء المشاهدة بأكثر من مصدر — السبب: `_videoListener` يستدعي `setState` على كل نبضة موضع (عدة مرات بالثانية) فيعيد بناء الشاشة كاملة حتى بلا أي تغيّر مرئي، ينافس فك التشفير/الرسم على نفس الإطار | إعادة البناء فقط عند تغيّر `isPlaying`/`isBuffering` فعلياً أو أثناء ظهور عناصر التحكم | `b0262a9` |
| 15 | حلقة مستوردة من أكثر من موقع (راجع AHMED-dashboard) — لا دعم لأكثر من مصدر ويب واحد؛ وزر "تغيير السيرفر" كان أصلاً معطوباً لجلسات ويب (يستدعي تجربة تشغيل أصلي على رابط صفحة ويب) لكن غير قابل للوصول سابقاً لعدم وجود أكثر من سيرفر لهذا النوع | `channels.sources[]` يصبح سيرفرات متعددة بـ`channel_source_resolver.dart`؛ تبديل يدوي صحيح + تبديل تلقائي عند فشل السيرفر الحالي كلياً (صفحة لم تفتح/انتهت مهلة الاكتشاف) | `488ce33` |
| — | **مؤجّل بطلب المستخدم**: زر بث لشاشة التلفاز (Google Cast) — يحتاج مكتبة Cast أصلية + تعديل AndroidManifest + اختبار على جهاز حقيقي (غير ممكن بهذه البيئة). قيد معروف: Chromecast يجلب الفيديو مباشرة من التلفاز بلا هيدرز الهاتف المخصصة (Referer/Origin/Cookie)، فيفشل لأي مصدر محمي بهذه الهيدرز إلا بطبقة وسيط عبر الووركر المشترك — لم تُنفَّذ بعد. | — |
| 16 | مؤكَّد بسجل تشخيص فعلي: مؤقت اكتشاف المرشحين بالخلفية يستمر بعد إثبات تشغيل WebView وعرضه للمستخدم، فيطلق أحياناً محاولة تشغيل أصلي تفشل (NATIVE_TRIAL_FAILED — staying on WebView) — `_playServerQuality` كان يفرض `_state=loading` بلا شرط عند البدء والفشل، فيغطي مؤشر تحميل الفيديو الشغّال فعلياً لثوانٍ، يبدو للمستخدم كـ"تعذر التشغيل" ثم يصلح نفسه | تجاهل فرض `_state=loading` (بدايةً وعند الفشل) لو `_webPlaybackReady` صحيحة أصلاً | `6521501` |
| 17 | شبكة ضعيفة تجعل ExoPlayer يرمي خطأ (`value.hasError`) تُظهر شاشة خطأ كاملة فوراً تحتاج ضغطة "إعادة المحاولة" يدوياً حتى لو كانت المشكلة عابرة | إعادة اتصال تلقائية بصمت (نفس السيرفر/الموضع) حتى 8 محاولات بتأخير متزايد؛ تتوقف فقط لو ضغط المستخدم زر الإيقاف بنفسه (`_userPausedPlayback`) أو استُنفدت المحاولات | `ad2cb6f` |
| 18 | أوضاع عرض الفيديو (5) تتضمن `fitWidth` الذي يقصّ من الأعلى والأسفل — يقصّ الترجمة المحروقة عند بعض المصادر | تقليص لـ4 أوضاع (حذف `fitWidth`) — الباقي إما بلا قصّ أو يقصّ من الجانبين فقط، لا يلمس الترجمة | `ad2cb6f` |
| 19 | زر الرجوع بالمشغّل (داخل صف التحكم العلوي) يتشارك فعلياً نفس منطقة الشاشة مع زري القفل/وضع العرض (`Positioned` منفصلة بأعلى يسار الشاشة) فيظهر خلفهما (مؤكَّد بلقطة شاشة من المستخدم) | نُقل لـ`Positioned` مستقلة بأعلى يمين الشاشة بدل داخل الصف | `ad2cb6f` |
| 20 | بناء APK فعلي (Android Studio) يفشل دائماً بـ"Compilation error" بمهمة `:image_gallery_saver_plus:compileReleaseKotlin` مع أدوات Gradle/Kotlin/AGP الحديثة (مؤكَّد من عدة محاولات بناء حقيقية) | ترقية `image_gallery_saver_plus` من 3.0.5 لـ4.0.1 — نفس واجهة Dart حرفياً (تحقّق من مصدر الحزمة)، لكن كوده الأندرويدي الداخلي يستخدم Kotlin/AGP أحدث وأكثر توافقاً | `9519a8f` |
| 21 | بعد إصلاح #20، بناء فعلي عبر Android Studio (Flutter 3.47.2 / Dart 3.13.2 الفعلي على جهاز المستخدم) يفشل بخطأ Dart compile حقيقي **داخل مكتبة `google_fonts` 6.3.0 نفسها** ("does not have a primitive operator ==" بخريطة const بـ`google_fonts_variant.dart`) — ليس خطأ بكودنا | ترقية `google_fonts` من 6.2.1 (قفل فعلي عند 6.3.0) لـ8.2.1 — نفس واجهة `tajawalTextTheme` حرفياً؛ بيئة الفحص هنا حُدِّثت لتطابق Flutter/Dart الحقيقي على جهاز المستخدم بالضبط (`git checkout 3.47.2` بمستودع Flutter) و`dart analyze` الكامل مرّ نظيفاً بهذا التطابق | `af36d45` |
| 22 | بعد إصلاح #20/#21، بناء محلي (بلا Codemagic/CI) يفشل بمرحلة `:app:packageRelease` الأخيرة: "SigningConfig 'release' is missing required property 'storeFile'" — `signingConfigs.release` بـ`android/app/build.gradle` كان يُملأ فقط لما `CI=true` (Codemagic يضبطه ويرفع الكيستور الحقيقي تلقائياً)، فيبقى فارغاً تماماً بأي بناء محلي على جهاز مطوّر | `buildTypes.release.signingConfig` يستخدم الآن `signingConfigs.debug` محلياً (بلا CI) بدلاً من الفشل الكامل — يكفي للتثبيت والتجربة المباشرة؛ بناء Codemagic الحقيقي غير متأثر | `7471dc9` |
| 23 | بعد نجاح البناء المحلي، APK "شامل" واحد بحجم 56.7 ميجا (بدل ~19 ميجا بـCodemagic) — `--split-per-abi` (ملف أصغر منفصل لكل معمارية) يفشل بتعارض Gradle صريح مع `ndk.abiFilters` اليدوي بـ`build.gradle` ("cannot be present when splits abi filters are set") | إزالة `ndk.abiFilters`، واستخدام `--target-platform android-arm,android-arm64` بدلاً منه (نفس التقييد لمعماريتين حقيقيتين فقط، متوافق مع `--split-per-abi`)؛ طُبِّق على `codemagic.yaml` أيضاً | `93f4956` |
| 24 | MIUI/HyperOS: "لا يتوافق هذا التطبيق مع أحدث إصدار من أندرويد" عند تثبيت بناء محلي موقَّع بمفتاح debug — استمرت حتى بعد حذف التطبيق بالكامل وإعادة التثبيت (ليست تعارض توقيع مع نسخة قديمة) | كيستور محلي حقيقي ثابت اختياري (`android/key.properties`، طبقة توقيع ثالثة بجانب debug ومفتاح Codemagic الحقيقي) — راجع `CLAUDE.md` لخطوات الإنشاء بـ`keytool` | `ccd4b56` |
| 25 | لا يوجد workflow بـ`codemagic.yaml` يبني App Bundle (.aab) — Google Play يرفض رفع أي إصدار جديد على تطبيق منشور بصيغة APK، يتطلب .aab حصراً | `workflow` جديد منفصل `release-bundle` (`flutter build appbundle --release`)؛ `android-workflow` الأصلي يبقى لبناء APK للتجربة المباشرة فقط. رُفع أيضاً رقم الإصدار (`1.0.5+6` → `1.0.6+7`) ليشمل كل إصلاحات هذه الجلسة | `b7afefe` |
| 26 | زر/شريط الصوت الدائم بشريط التحكم — طلب صريح لتجربة "مشغلات كبرى" (MX Player): سحب رأسي فقط + شارة مؤقتة تختفي تلقائياً، بلا زر دائم | حذف الزر والشريط، شارة `_showCenterToast` (🔊/🔇 + نسبة) بنفس نمط شارة التكبير الموجودة | `7dbd0e4` |
| 27 | إعادة الاتصال التلقائي (راجع #17) كانت تنتظر 1-5 ثوانٍ كاملة — طلب استجابة شبه فورية | تخفيض التأخير لـ200-2000 ملي ثانية فقط | `7dbd0e4` |
| 28 | تبديل **الجودة** بقائمة `_openQualitySheet` (خلاف تبديل السيرفر، مُصلَح سابقاً بـ`488ce33`) كان يستدعي `_playServerQuality` (تشغيل أصلي مباشر) دائماً حتى لو كانت الجلسة `StreamKind.web` — رابط الجودة بهذه الحالة صفحة ويب لا رابط وسائط، فلا يتغيّر شيء فعلياً عند الاختيار | نفس فحص `session.kind == StreamKind.web` المستخدَم عند تبديل السيرفر، مطبَّق الآن على تبديل الجودة أيضاً (`_openWebSource` بدل `_playServerQuality`) | `7dbd0e4` |
| — | **معلّق، يحتاج سجل تشخيص فعلي للتأكيد**: أبلغ المستخدم أن مصدراً معيّناً (يظهر بحالة "تم تشغيل المصدر بالخلفية" — أي جلسة WebView لم ينجح التشغيل الأصلي معها) يعرض بواجهة قائمة جودات (video.js/JWPlayer الأرجح، لا واجهتنا) لا تغيّر شيئاً فعلياً — الإصلاح أعلاه (#28) يغطي حالة مشابهة **لو** كانت القائمة من واجهتنا نحن، لكن لم يُؤكَّد بعد أن هذا هو نفس السيناريو المُبلَّغ عنه بدون سجل فعلي لنفس الجلسة | — |
| 29 | **فهم خاطئ لطلب المستخدم**: فُهم أول مرة إن المطلوب حذف شاشة "تم تشغيل المصدر في الخلفية" بالكامل — الصحيح (وضّحه المستخدم لاحقاً): يريد حذف **نص الإشعار فقط** ("تم تشغيل المصدر في الخلفية")، ويريد إبقاء الصفحة السوداء نفسها (تخفي صفحة الويب الفوضوية عن المستخدم وتمنع لمسه العرضي لإعلانات أثناء الاكتشاف الصامت — تجربة أفضل عمداً) | حُذفت الشاشة بالكامل بـ`0c9e56d` (خطأ)، ثم أُرجعت كاملة (`git revert`) وحُذف فقط نص العنوان "تم تشغيل المصدر في الخلفية" من `_buildHiddenWebSourceStatus` — بقي الأيقونة+النص التوضيحي+زر "إظهار صفحة المصدر" كما كانوا | `0c9e56d` (رُجع)، (كوميت تصحيح لاحق) |
| 30 | **تراجع بسبب بلاغ مستخدم حقيقي بانحدار أداء**: أُضيف وكيل HLS محلي (`hls_cache_proxy.dart`) لمعالجة التقطيع (راجع التفاصيل بتاريخ الكوميت `0faf83d` بالـgit log) — بعد الاختبار الفعلي على الهاتف أبلغ المستخدم أن المشغّل "لا يلتقط بسرعة المصادر ويتأخر ولا يشغّل تلقائياً"، وسجل تشخيص فعلي بعدها أظهر محاولة تشغيل أصلي (`PLAY_SERVER_QUALITY_START`) لم تصل لا لنجاح ولا لفشل مسجَّل خلال +43 ثانية كاملة بينما نفس الرابط نجح سابقاً (بسجلات أخرى) خلال ~20 ثانية بدون الوكيل — دليل مباشر إن الوكيل (غير مختبَر فعلياً على أندرويد حقيقي قبل هذا) على الأرجح يعلّق التهيئة بدل تسريعها | حُذف الوكيل بالكامل (الملف + كل نقاط استخدامه بـ`_playServerQuality`/`dispose`) — رجوع كامل لبناء `VideoPlayerController` المباشر كما كان قبل هذا التعديل. الدرس: لا تُترك تغييرات شبكة/native غير مختبَرة فعلياً حيّة بمجرد بلاغ مستخدم بمشكلة، حتى لو غير مؤكّدة السبب 100%— التراجع الآمن أولى من تكديس إصلاحات فوق تغيير مشكوك فيه | (كوميت تصحيح لاحق) |
| 31 | مؤكَّد بسجل تشخيص فعلي (`NATIVE_TRIAL_DIAGNOSTIC status=400`): موقع (grzcdn.com) يسجّل رابط الـmanifest بإعداد مشغّله الخاص (JWPlayer) بشرطة مائلة زائدة مباشرة بعد `.m3u8` — الرابط بهذا الشكل يرجّع "400 Bad Request" حرفياً من nginx، بينما الاكتشاف عبر XHR/fetch الحقيقي يرصد نفس الرابط بلا الشرطة ويعمل بنجاح. النتيجة: أول محاولة تشغيل أصلي (من أصل 2 فقط بميزانية المحاولات) كانت تُهدر دائماً على الشكل الفاسد + معالج التشخيص (`_logNativeFailureDiagnostics`) يهدر ثوانٍ إضافية قبل أن تنجح المحاولة الثانية بالشكل الصحيح | حذف أي شرطة مائلة زائدة مباشرة بعد `.m3u8`/`.m3u` داخل `_normalizeCandidate` — يوحّد أيضاً مفتاح تسجيل المرشّح بالـregistry بدل معاملة الشكلين كمصدرين مختلفين، فيمنع هدر أول محاولة على الشكل الفاسد من الأساس | (كوميت لاحق) |
