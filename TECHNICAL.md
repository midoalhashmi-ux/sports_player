# التوثيق التقني — sports_player (تطبيق المشغل)

> هذا الملف مرجع تقني شامل لهذا المستودع، بحيث يقدر أي مبرمج أو نموذج ذكاء
> اصطناعي آخر يفهم بنية المشروع كاملة بدون الحاجة لقراءة كل الكود من الصفر.
> **كل تعديل جوهري أو إصلاح مشكلة لاحق يُضاف كصف جديد في جدول "سجل المشاكل
> والحلول" بالأسفل** — سطر واحد مختصر لكل عمود، بدون كود أو شرح مطوّل
> (التفاصيل الكاملة موجودة برسالة الكوميت نفسها، هذا الجدول فهرس سريع فقط).

## 1. نظرة عامة على المنظومة الكاملة (3 مستودعات مترابطة)

هذا المشروع جزء من منظومة من 3 مستودعات منفصلة على GitHub تتشارك نفس
مشروع Firebase ونفس Cloudflare Worker:

| المستودع | الدور |
|---|---|
| **BinSheikh** | تطبيق المحتوى الرئيسي (Flutter/Android) — يعرض للمستخدم النهائي الأقسام/القنوات/الأفلام/المسلسلات/الأنمي/مباريات اليوم. |
| **sports_player** (هذا المستودع) | تطبيق Flutter/Android منفصل، وظيفته الوحيدة تشغيل الفيديو الفعلي. يُفتح من BinSheikh عبر `app_links` بمعرّف قناة/حلقة. فصله كتطبيق مستقل يتيح تحديثه في Play Store بدون أي حاجة لتحديث BinSheikh، ويعزل منطق حل الروابط الحقيقية/الحماية عن تطبيق المحتوى العام. |
| **AHMED-dashboard** | لوحة تحكم ويب ثابتة (HTML/JS/CSS خام، بدون أي أداة بناء) يديرها فريق المحتوى — إضافة/تعديل الأقسام والقنوات والروابط، ومتابعة إحصائيات المشاهدة. |

- **مشروع Firebase المشترك**: `sports-stream-app-36a7a` (نفس Firestore، نفس
  Auth) — راجع `lib/firebase_options.dart`.
- **Cloudflare Worker المشترك**: `https://binsheikh-api.binsheikh.workers.dev`
  (كوده المصدري داخل مستودع BinSheikh في `cloudflare-worker/`) — يخدم روابط
  البث الحقيقية ومباريات اليوم والتقييمات واستيراد المواقع.

## 2. هذا المستودع (sports_player)

- **الغرض**: تطبيق Flutter/Android، وظيفته الوحيدة تشغيل فيديو HLS/MP4 لقناة
  أو حلقة معيّنة يُمرَّر إليه معرّفها.
- **Flutter SDK المستخدم**: 3.27.0 (حسب `codemagic.yaml`).
- **التبعيات الأساسية** (`pubspec.yaml`):
  - `video_player` — التشغيل الأصلي (ExoPlayer على أندرويد)، يدعم HLS وMP4
    فقط عملياً (لا DASH/RTMP/MKV رغم ما كان مكتوباً سابقاً بالكود).
  - `webview_flutter` — لعرض صفحات بث كويب عند `streamType=web`.
  - `firebase_core` + `cloud_firestore`.
  - `http` — لطلبات StreamAuthService والتشخيص.
  - `wakelock_plus`, `google_mobile_ads`, `share_plus`, `url_launcher`,
    `package_info_plus`, `shared_preferences`, `image_gallery_saver_plus`.

### بنية `lib/`

- `main.dart` — نقطة الدخول: تهيئة Firebase + معالج أخطاء عام
  (`runZonedGuarded` + `FlutterError.onError` + `PlatformDispatcher.onError`)
  يسجّل أي خطأ غير ملتقط عبر `SessionLogService`.
- `screens/watch_screen.dart` (~4700 سطر) — **الشاشة الأهم والأكبر**. آلة
  حالة كاملة لتشغيل الفيديو: تجربة عدة سيرفرات/جودات (`_WebNetworkCandidate`)،
  التبديل بين تشغيل native (`video_player`) وWebView، اكتشاف تلقائي لمصدر
  الفيديو من صفحة ويب (`_autoDetectWebSource`) عبر تتبّع طلبات شبكة داخل
  WebView، وتسجيل أحداث تشخيصية عبر `SessionLogService` (`_slog`) لتحليلها
  لاحقاً من سجل مُصدَّر (راجع مهارة `stream-debug` إن وُجدت في
  `.claude/skills/`).
- `screens/home_screen.dart`, `add_url_screen.dart`, `contact_screen.dart`,
  `force_update_screen.dart`, `terms_privacy_screen.dart` — شاشات مساعدة.
- `services/stream_auth_service.dart` — يطلب جلسة بث من الووركر
  (`POST /getStreamUrl`)، يدعم شكل استجابة متعدد السيرفرات/الجودات وشكل قديم
  مبسّط (`{url, expiresIn}`) لتوافق رجعي.
- `services/channel_source_resolver.dart`, `api_source_resolver.dart` — حل
  مصدر القناة (رابط مباشر مخزَّن أو مصدر API ديناميكي).
- `services/session_log_service.dart` — يجمع سجلاً نصياً زمنياً لكل جلسة
  تشغيل، قابلاً للتصدير — هذا هو أساس التشخيص عن بعد بدون وصول فعلي لجهاز
  المستخدم.
- `services/native_cookie_service.dart`, `worker_config.dart`,
  `version_check_service.dart`, `feature_flags_service.dart`,
  `pip_service.dart`, `player_visibility_service.dart`,
  `saved_link_service.dart`, `ad_service.dart`.
- `theme/app_theme.dart` — خط Tajawal + الألوان الموحّدة.

## 3. تدفق تشغيل الفيديو (مختصر)

1. يُفتح `watch_screen` بمعرّف قناة/حلقة (`channelId`) من BinSheikh عبر
   `app_links`، أو يدوياً من `add_url_screen`.
2. `StreamAuthService.requestSession(channelId)` → `POST /getStreamUrl` على
   الووركر.
3. الووركر يتحقق من Firestore (`channels` + `privateStreams`) ويرجّع إما
   رابطاً واحداً أو عدة سيرفرات/جودات.
4. `watch_screen` يجرّب كل مصدر بالترتيب: تشغيل native أولاً
   (`video_player`/ExoPlayer)، ولو فشل أو كان `streamType=web` يحوّل لـ
   WebView مع اكتشاف تلقائي للمصدر الحقيقي من طلبات الشبكة.
5. كل خطوة تُسجَّل عبر `SessionLogService` — هذا هو السجل الذي يُصدَّر من
   التطبيق ويُستخدم للتشخيص.

## 4. الحماية الأمنية

روابط m3u8 الحقيقية **غير مخزّنة إطلاقاً** بهذا التطبيق ولا تُقرأ مباشرة من
Firestore — تُجلب فقط عبر `/getStreamUrl` بالووركر (الذي يقرأ
`privateStreams` بصلاحيات خاصة لا تصل لأي عميل).

## سجل المشاكل والحلول

| # | المشكلة | الحل | الكوميت |
|---|---|---|---|
| 1 | رابط بمسار مزدوج `//` (`vidtube.cam//`) لا يُكتشف كـ"غير وسائط" | تعميم `_isNonMediaAsset` ليطابق أي عدد شرطات، لا `/` فقط | `995a558` |
| 2 | حالة `nativePlaying` تُستبدل بصمت بعد نجاح التشغيل (يعيد اكتشاف الويب) | حارس مركزي داخل `_setWebSessionState` يرفض مغادرتها | `d6d1d28` |
| 3 | كود ميت + تحذيرات lint (3 ملفات غير مستخدمة، حقول ميتة...) | حذف/تنظيف + `analysis_options.yaml` (`flutter_lints`) | `3c9e51c` |
| 4 | لا معالج أخطاء عام — خطأ غير ملتقط يُسقط التطبيق بصمت | `runZonedGuarded`+`FlutterError.onError`+`PlatformDispatcher.onError` بـ`main.dart` | نفس `3c9e51c` |
| 5 | تعذّر تشخيص "Source error" من ExoPlayer بالسجل النصي وحده | تسجيل أحداث buffering/error + فحص HTTP Range تشخيصي عند فشل native | `1eae933`, `721cde3` |
| 6 | إخفاء صفحة المصدر (إعداد إلزامي) أقل موثوقية من إظهارها لمواقع JWPlayer/Video.js — Chromium لا يحتسب نقرة JS محاكاة كنقرة حقيقية لـautoplay | كشف مشغّلات عام بالخاصية (لا بالدومين) + كتم صوت أثناء التحضير بالإخفاء + رفض ادّعاء "جاهز" بدون دليل تشغيل حقيقي | `4b7cda6` |
| 7 | `_normalizeCandidate` يضيف `#` زائدة لكل رابط مصدر (حتى بلا fragment أصلاً) — يفسد مطابقة سجل المرشحين/منع التكرار | `uri.replace(fragment:'')` ↔ `uri.removeFragment()` (مؤكَّد باختبار فعلي على SDK 3.27.0) | `1387fc1` |
| 8 | فشل متكرر `ExoPlaybackException: Source error` لمصادر HLS حقيقية تعمل بنجاح داخل WebView (مؤكَّد بسجلَين حقيقيَين — grzcdn.com) — السبب: رابط المرشّح يصل لمرحلة التشغيل الأصلي أحياناً بـ`/` إضافية لم تكن موجودة وقت تسجيله، فيفشل بحث `_webCandidateRegistry` ويسقط `formatHintOverride` لـ`null`، فيحاول ExoPlayer تخمين نوع الملف من امتداد الرابط ويفشل لأنه لا ينتهي بـ`.m3u8` حرفياً | تمرير تلميح HLS صريح كلما كان `strongHls` (نمط الرابط، مثل مقطع `/hls/`) يثق بالمصدر أصلاً — كان محسوباً مسبقاً بنفس الدالة ولم يُستخدم هنا | `dfcf47f` |
| 9 | زر وضع العرض محدود بـ3 خيارات فقط — لا يكفي كل نِسب الشاشات | إضافة `fitWidth`/`fitHeight` (5 خيارات الآن) | `de0aaa4` |
| 10 | `didChangeAppLifecycleState` يتجاهل تشغيل WebView تماماً (شائع جداً — كل مواقع JWPlayer/Video.js المعروفة) — عند تصغير التطبيق يستمر صوت/فيديو الصفحة وتنقّلها الإعلاني بالخلفية بصمت | إيقاف/استئناف عناصر `<video>/<audio>` بالـWebView بنفس منطق المشغّل الأصلي | `de0aaa4` |
| 11 | تخزين مؤقت (buffering) بلا تقدّم أثناء المشاهدة الفعلية قد يستمر للأبد بدون أي مخرج للمستخدم غير الخروج يدوياً | مراقب: محاولة إنعاش (seekTo) بعد 20 ثانية، وإذا فشلت الانتقال لشاشة الخطأ الموجودة (بزر إعادة المحاولة) بعد 20 ثانية إضافية | `de0aaa4` |
| 12 | قائمة وضع العرض المنسدلة تُخفي خياراتها السفلية خارج الشاشة على بعض الأجهزة (بلا إمكانية سحب لأعلى) | تبديل بنقرة واحدة (تدوير على الأوضاع) بدل قائمة، مع شارة نصية مؤقتة وسط الشاشة باسم الوضع | `b0262a9` |
| 13 | لا تكبير/تصغير بإصبعين (Pinch-to-zoom) بمشغّل الفيديو | أُضيف (100%=الحجم الطبيعي، شارة نسبة مؤقتة) — تطلّب توحيد إيماءات التقديم/الصوت الحالية مع onScale* لأن Flutter يمنع خلطها بـonHorizontalDrag*/onVerticalDrag* على نفس الأداة | `b0262a9` |
| 14 | تقطيع خفيف متكرر أثناء المشاهدة بأكثر من مصدر — السبب: `_videoListener` يستدعي `setState` على كل نبضة موضع (عدة مرات بالثانية) فيعيد بناء الشاشة كاملة حتى بلا أي تغيّر مرئي، ينافس فك التشفير/الرسم على نفس الإطار | إعادة البناء فقط عند تغيّر `isPlaying`/`isBuffering` فعلياً أو أثناء ظهور عناصر التحكم | `b0262a9` |
| 15 | حلقة مستوردة من أكثر من موقع (راجع AHMED-dashboard) — لا دعم لأكثر من مصدر ويب واحد؛ وزر "تغيير السيرفر" كان أصلاً معطوباً لجلسات ويب (يستدعي تجربة تشغيل أصلي على رابط صفحة ويب) لكن غير قابل للوصول سابقاً لعدم وجود أكثر من سيرفر لهذا النوع | `channels.sources[]` يصبح سيرفرات متعددة بـ`channel_source_resolver.dart`؛ تبديل يدوي صحيح + تبديل تلقائي عند فشل السيرفر الحالي كلياً (صفحة لم تفتح/انتهت مهلة الاكتشاف) | `488ce33` |
| — | **مؤجّل بطلب المستخدم**: زر بث لشاشة التلفاز (Google Cast) — يحتاج مكتبة Cast أصلية + تعديل AndroidManifest + اختبار على جهاز حقيقي (غير ممكن بهذه البيئة). قيد معروف: Chromecast يجلب الفيديو مباشرة من التلفاز بلا هيدرز الهاتف المخصصة (Referer/Origin/Cookie)، فيفشل لأي مصدر محمي بهذه الهيدرز إلا بطبقة وسيط عبر الووركر المشترك — لم تُنفَّذ بعد. | — |
| 16 | مؤكَّد بسجل تشخيص فعلي: مؤقت اكتشاف المرشحين بالخلفية يستمر بعد إثبات تشغيل WebView وعرضه للمستخدم، فيطلق أحياناً محاولة تشغيل أصلي تفشل (NATIVE_TRIAL_FAILED — staying on WebView) — `_playServerQuality` كان يفرض `_state=loading` بلا شرط عند البدء والفشل، فيغطي مؤشر تحميل الفيديو الشغّال فعلياً لثوانٍ، يبدو للمستخدم كـ"تعذر التشغيل" ثم يصلح نفسه | تجاهل فرض `_state=loading` (بدايةً وعند الفشل) لو `_webPlaybackReady` صحيحة أصلاً | `6521501` |
| 17 | شبكة ضعيفة تجعل ExoPlayer يرمي خطأ (`value.hasError`) تُظهر شاشة خطأ كاملة فوراً تحتاج ضغطة "إعادة المحاولة" يدوياً حتى لو كانت المشكلة عابرة | إعادة اتصال تلقائية بصمت (نفس السيرفر/الموضع) حتى 8 محاولات بتأخير متزايد؛ تتوقف فقط لو ضغط المستخدم زر الإيقاف بنفسه (`_userPausedPlayback`) أو استُنفدت المحاولات | `ad2cb6f` |
| 18 | أوضاع عرض الفيديو (5) تتضمن `fitWidth` الذي يقصّ من الأعلى والأسفل — يقصّ الترجمة المحروقة عند بعض المصادر | تقليص لـ4 أوضاع (حذف `fitWidth`) — الباقي إما بلا قصّ أو يقصّ من الجانبين فقط، لا يلمس الترجمة | `ad2cb6f` |
| 19 | زر الرجوع بالمشغّل (داخل صف التحكم العلوي) يتشارك فعلياً نفس منطقة الشاشة مع زري القفل/وضع العرض (`Positioned` منفصلة بأعلى يسار الشاشة) فيظهر خلفهما (مؤكَّد بلقطة شاشة من المستخدم) | نُقل لـ`Positioned` مستقلة بأعلى يمين الشاشة بدل داخل الصف | `ad2cb6f` |
| 20 | بناء APK فعلي (Android Studio) يفشل دائماً بـ"Compilation error" بمهمة `:image_gallery_saver_plus:compileReleaseKotlin` مع أدوات Gradle/Kotlin/AGP الحديثة (مؤكَّد من عدة محاولات بناء حقيقية) | ترقية `image_gallery_saver_plus` من 3.0.5 لـ4.0.1 — نفس واجهة Dart حرفياً (تحقّق من مصدر الحزمة)، لكن كوده الأندرويدي الداخلي يستخدم Kotlin/AGP أحدث وأكثر توافقاً | `9519a8f` |
| 21 | بعد إصلاح #20، بناء فعلي عبر Android Studio (Flutter 3.47.2 / Dart 3.13.2 الفعلي على جهاز المستخدم) يفشل بخطأ Dart compile حقيقي **داخل مكتبة `google_fonts` 6.3.0 نفسها** ("does not have a primitive operator ==" بخريطة const بـ`google_fonts_variant.dart`) — ليس خطأ بكودنا | ترقية `google_fonts` من 6.2.1 (قفل فعلي عند 6.3.0) لـ8.2.1 — نفس واجهة `tajawalTextTheme` حرفياً؛ بيئة الفحص هنا حُدِّثت لتطابق Flutter/Dart الحقيقي على جهاز المستخدم بالضبط (`git checkout 3.47.2` بمستودع Flutter) و`dart analyze` الكامل مرّ نظيفاً بهذا التطابق | `af36d45` |
| 22 | بعد إصلاح #20/#21، بناء محلي (بلا Codemagic/CI) يفشل بمرحلة `:app:packageRelease` الأخيرة: "SigningConfig 'release' is missing required property 'storeFile'" — `signingConfigs.release` بـ`android/app/build.gradle` كان يُملأ فقط لما `CI=true` (Codemagic يضبطه ويرفع الكيستور الحقيقي تلقائياً)، فيبقى فارغاً تماماً بأي بناء محلي على جهاز مطوّر | `buildTypes.release.signingConfig` يستخدم الآن `signingConfigs.debug` محلياً (بلا CI) بدلاً من الفشل الكامل — يكفي للتثبيت والتجربة المباشرة؛ بناء Codemagic الحقيقي غير متأثر | `7471dc9` |
| 23 | بعد نجاح البناء المحلي، APK "شامل" واحد بحجم 56.7 ميجا (بدل ~19 ميجا بـCodemagic) — `--split-per-abi` (ملف أصغر منفصل لكل معمارية) يفشل بتعارض Gradle صريح مع `ndk.abiFilters` اليدوي بـ`build.gradle` ("cannot be present when splits abi filters are set") | إزالة `ndk.abiFilters`، واستخدام `--target-platform android-arm,android-arm64` بدلاً منه (نفس التقييد لمعماريتين حقيقيتين فقط، متوافق مع `--split-per-abi`)؛ طُبِّق على `codemagic.yaml` أيضاً | `93f4956` |
| 24 | MIUI/HyperOS: "لا يتوافق هذا التطبيق مع أحدث إصدار من أندرويد" عند تثبيت بناء محلي موقَّع بمفتاح debug — استمرت حتى بعد حذف التطبيق بالكامل وإعادة التثبيت (ليست تعارض توقيع مع نسخة قديمة) | كيستور محلي حقيقي ثابت اختياري (`android/key.properties`، طبقة توقيع ثالثة بجانب debug ومفتاح Codemagic الحقيقي) — راجع `CLAUDE.md` لخطوات الإنشاء بـ`keytool` | `ccd4b56` |
| 25 | لا يوجد workflow بـ`codemagic.yaml` يبني App Bundle (.aab) — Google Play يرفض رفع أي إصدار جديد على تطبيق منشور بصيغة APK، يتطلب .aab حصراً | `workflow` جديد منفصل `release-bundle` (`flutter build appbundle --release`)؛ `android-workflow` الأصلي يبقى لبناء APK للتجربة المباشرة فقط. رُفع أيضاً رقم الإصدار (`1.0.5+6` → `1.0.6+7`) ليشمل كل إصلاحات هذه الجلسة | `b7afefe` |
| 26 | زر/شريط الصوت الدائم بشريط التحكم — طلب صريح لتجربة "مشغلات كبرى" (MX Player): سحب رأسي فقط + شارة مؤقتة تختفي تلقائياً، بلا زر دائم | حذف الزر والشريط، شارة `_showCenterToast` (🔊/🔇 + نسبة) بنفس نمط شارة التكبير الموجودة | `7dbd0e4` |
| 27 | إعادة الاتصال التلقائي (راجع #17) كانت تنتظر 1-5 ثوانٍ كاملة — طلب استجابة شبه فورية | تخفيض التأخير لـ200-2000 ملي ثانية فقط | `7dbd0e4` |
| 28 | تبديل **الجودة** بقائمة `_openQualitySheet` (خلاف تبديل السيرفر، مُصلَح سابقاً بـ`488ce33`) كان يستدعي `_playServerQuality` (تشغيل أصلي مباشر) دائماً حتى لو كانت الجلسة `StreamKind.web` — رابط الجودة بهذه الحالة صفحة ويب لا رابط وسائط، فلا يتغيّر شيء فعلياً عند الاختيار | نفس فحص `session.kind == StreamKind.web` المستخدَم عند تبديل السيرفر، مطبَّق الآن على تبديل الجودة أيضاً (`_openWebSource` بدل `_playServerQuality`) | `7dbd0e4` |
| — | **معلّق، يحتاج سجل تشخيص فعلي للتأكيد**: أبلغ المستخدم أن مصدراً معيّناً (يظهر بحالة "تم تشغيل المصدر بالخلفية" — أي جلسة WebView لم ينجح التشغيل الأصلي معها) يعرض بواجهة قائمة جودات (video.js/JWPlayer الأرجح، لا واجهتنا) لا تغيّر شيئاً فعلياً — الإصلاح أعلاه (#28) يغطي حالة مشابهة **لو** كانت القائمة من واجهتنا نحن، لكن لم يُؤكَّد بعد أن هذا هو نفس السيناريو المُبلَّغ عنه بدون سجل فعلي لنفس الجلسة | — |
| 29 | **فهم خاطئ لطلب المستخدم (تكرّر مرتين)**: (أ) فُهم أول مرة إن المطلوب حذف شاشة "تم تشغيل المصدر في الخلفية" بالكامل — الصحيح: حذف نص الإشعار فقط، مع إبقاء الصفحة السوداء نفسها (تخفي صفحة الويب الفوضوية عن المستخدم وتمنع لمسه العرضي لإعلانات أثناء الاكتشاف الصامت — تجربة أفضل عمداً). (ب) بعد إرجاع الصفحة بأيقونة+نص توضيحي+زر "إظهار صفحة المصدر"، وضّح المستخدم أنه يريد **سواداً كاملاً بلا أي شيء ظاهر إطلاقاً** — لا أيقونة ولا نص ولا زر | (أ) حُذفت الشاشة بالكامل بـ`0c9e56d` (خطأ)، أُرجعت كاملة (`git revert`) وحُذف نص العنوان فقط. (ب) بعدها بُسِّطت `_buildHiddenWebSourceStatus` لتصبح `ColoredBox(color: Colors.black)` فقط بلا أي محتوى — حُذف أيضاً `_revealWebPageForInteraction` (صار كوداً ميتاً بلا أي مستدعٍ بعد حذف الزر) | `0c9e56d` (رُجع)، (كوميتات تصحيح لاحقة) |
| 30 | **تراجع بسبب بلاغ مستخدم حقيقي بانحدار أداء**: أُضيف وكيل HLS محلي (`hls_cache_proxy.dart`) لمعالجة التقطيع (راجع التفاصيل بتاريخ الكوميت `0faf83d` بالـgit log) — بعد الاختبار الفعلي على الهاتف أبلغ المستخدم أن المشغّل "لا يلتقط بسرعة المصادر ويتأخر ولا يشغّل تلقائياً"، وسجل تشخيص فعلي بعدها أظهر محاولة تشغيل أصلي (`PLAY_SERVER_QUALITY_START`) لم تصل لا لنجاح ولا لفشل مسجَّل خلال +43 ثانية كاملة بينما نفس الرابط نجح سابقاً (بسجلات أخرى) خلال ~20 ثانية بدون الوكيل — دليل مباشر إن الوكيل (غير مختبَر فعلياً على أندرويد حقيقي قبل هذا) على الأرجح يعلّق التهيئة بدل تسريعها | حُذف الوكيل بالكامل (الملف + كل نقاط استخدامه بـ`_playServerQuality`/`dispose`) — رجوع كامل لبناء `VideoPlayerController` المباشر كما كان قبل هذا التعديل. الدرس: لا تُترك تغييرات شبكة/native غير مختبَرة فعلياً حيّة بمجرد بلاغ مستخدم بمشكلة، حتى لو غير مؤكّدة السبب 100%— التراجع الآمن أولى من تكديس إصلاحات فوق تغيير مشكوك فيه | (كوميت تصحيح لاحق) |
| 31 | مؤكَّد بسجل تشخيص فعلي (`NATIVE_TRIAL_DIAGNOSTIC status=400`): موقع (grzcdn.com) يسجّل رابط الـmanifest بإعداد مشغّله الخاص (JWPlayer) بشرطة مائلة زائدة مباشرة بعد `.m3u8` — الرابط بهذا الشكل يرجّع "400 Bad Request" حرفياً من nginx، بينما الاكتشاف عبر XHR/fetch الحقيقي يرصد نفس الرابط بلا الشرطة ويعمل بنجاح. النتيجة: أول محاولة تشغيل أصلي (من أصل 2 فقط بميزانية المحاولات) كانت تُهدر دائماً على الشكل الفاسد + معالج التشخيص (`_logNativeFailureDiagnostics`) يهدر ثوانٍ إضافية قبل أن تنجح المحاولة الثانية بالشكل الصحيح | حذف أي شرطة مائلة زائدة مباشرة بعد `.m3u8`/`.m3u` داخل `_normalizeCandidate` — يوحّد أيضاً مفتاح تسجيل المرشّح بالـregistry بدل معاملة الشكلين كمصدرين مختلفين، فيمنع هدر أول محاولة على الشكل الفاسد من الأساس | (كوميت لاحق) |
| 32 | طلب صريح من المستخدم: قائمة الجودات ("الجودة") لا تظهر إطلاقاً بالمشغّل للمصادر المُكتشَفة تلقائياً من مواقع الاستيراد (أفلام/مسلسلات/أنمي) — السبب: كل مرشّح مُكتشَف عبر `_autoDetectWebSource` يُبنى بجودة وهمية واحدة فقط (`StreamServerOption(qualities: [quality])`)، فشرط إظهار القائمة (`qualities.length > 1`) لا يتحقق أبداً بغض النظر عن كون playlist المصدر فعلياً متعدد الجودات (`#EXT-X-STREAM-INF`) | إضافة `_parseHlsVariantQualities`/`_populateHlsQualitiesInBackground`: بعد نجاح التشغيل فعلياً (لا قبله — لتفادي أي تأخير على أول محاولة، نفس درس الحالة C بـ`known-bugs.md`)، يُجلب نص الـmanifest بالخلفية ويُحلَّل بحثاً عن جودات بديلة حقيقية (دقة/معدل نقل)، فتُستبدل الجودة الوهمية الواحدة بقائمة حقيقية ("تلقائي" + كل دقة صراحة) إن وُجدت. حلّ عام يعمل مع أي مصدر HLS متعدد الجودات، لا يخص موقعاً معيّناً. عولجت أيضاً ثغرة توجيه: تبديل جودة لسيرفر "المصدر المكتشف تلقائياً" (`_autoDiscoveredServerLabel`) يذهب مباشرة لـ`_playServerQuality` بدل `_openWebSource` حتى لو `session.kind == StreamKind.web` — روابطه دائماً وسائط مُثبَتة لا صفحات ويب، خلافاً لسيرفرات CMS متعددة المصادر (`channels.sources[]`) التي تبقى بمنطقها القديم دون تغيير | (كوميت لاحق) |
| 33 | إعادة محاولة #30 (وكيل HLS المحلي، تراجعنا عنه بعد تعليق تشغيل فعلي بلا تشخيص) — المستخدم طلب حلاً جذرياً للتقطيع بعد التأكد أن `isLive` الداخلي بالتطبيق لا يتحكم بسلوك ExoPlayer إطلاقاً (UI فقط)، مع احتمال حقيقي غير مؤكَّد أن بعض المواقع لا تضع `#EXT-X-ENDLIST` فيُعامَل المحتوى كبث مباشر عند ExoPlayer رغم كونه حلقة كاملة — لا يوجد أي تحكم مباشر بهذا عبر `video_player` العامة | أُعيد `hls_cache_proxy.dart` بطبقتي أمان لم تكونا موجودتين بالمحاولة الأولى: (أ) اختبار ذاتي فوري داخل `start()` (جلب `/pl` من عندنا نحن أولاً) — فشل الاختبار يعني الرجوع للرابط المباشر دون تسليم أي رابط غير مستجيب لـExoPlayer إطلاقاً. (ب) تسجيل كامل (`onLog` → `_slog`) لكل حدث (بدء الوكيل، نتيجة الاختبار الذاتي، كل فشل جلب) — أي تعليق لاحق يكون قابلاً للتشخيص من سجل حقيقي، لا تخمينًا. أُضيف أيضاً حد أقصى عام **15 ثانية** على `newController.initialize()` نفسها بـ`_playServerQuality` (بغض النظر عن الوكيل) — أي تعليق فعلي يتحوّل الآن لفشل واضح مسجَّل خلال 15 ثانية كحد أقصى بدل تعليق صامت لأكثر من 40 ثانية كما حصل فعلياً بالمحاولة السابقة. **يحتاج تجربة فعلية وسجل جديد للتأكد** — لو تكرر التعليق، ابحث بالسجل عن `HLS_PROXY_SELFTEST_FAILED`/`HLS_PROXY_*_ERROR` (يوضّح فشل الوكيل نفسه) أو رسالة `native initialize() timed out` (يوضّح تعليقاً بمكان آخر غير الوكيل) | (كوميت لاحق) |
| 34 | طلب صريح: تجربة استخدام أقرب لتطبيق يوتيوب (سلاسة، استجابة، شكل الأزرار) — "99% لا 100%" | تعديلات شكل/حركة بحتة بلا أي مكتبة جديدة: (أ) تأثير نقر مزدوج جديد بالكامل — لوحة نصف-دائرية تلتصق بحافة الشاشة وتنبض عند كل نقرة سريعة إضافية بنفس الجهة مع تراكم الثواني ("+10"←"+20"←"+30"...) بدل أيقونة ثابتة لا تتحرك. (ب) خلفية دوائر الأزرار خُفِّفت كثيراً (0.35→0.15 شفافية) ومسافاتها حول زر التشغيل الأوسط تقلّصت (22px→12px) لشكل أنظف. (ج) شريط التقدّم أرفع افتراضياً (3px→2px) والكرة **تظهر فقط أثناء السحب الفعلي** (`onChangeStart`/`onChangeEnd`) بدل ظهور دائم. (د) حركة انكماش خفيفة (`_BouncyPress`) عند الضغط على زر التشغيل الأوسط تحديداً لإحساس استجابة فورية. (هـ) **اسم الحلقة/الفيلم أعلى الشاشة** — يتطلّب تعديل عابر للمستودعين: `WatchScreen.title` جديد (اختياري بالكامل، غيابه بلا أي أثر) يُمرَّر إما من رابط عميق (`main.dart` يقرأ `title=` من الـquery params) أو من رابط محفوظ يدوياً (`SavedLink.title`)؛ بجانب BinSheikh (`PlayerLauncher.openChannel` يضيف `&title=` من `channel.title`, `ChannelCard` يمرّره) | (كوميت لاحق، مستودعان) |
| 35 | مؤكَّد بسجلَي "فيلم يفتح ويقفل" حقيقيَين: إعادة اتصال تلقائي بعد قطع CDN عابر (راجع #17) كانت تقلب `_state=loading` بلا شرط بكل محاولة من الثماني، تهدم عرض الفيديو الشغّال فعلياً وتستبدله بشاشة تحميل سوداء كاملة كل مرة — يظهر للمستخدم كتقطيع "فتح وإغلاق" متكرر رغم أن التشغيل غالباً يتعافى فوراً | نفس حارس #16 (`!(fallbackToWeb && _webPlaybackReady)`) مطبَّق الآن أيضاً على الحالة العكسية: `wasAlreadyPlayingNative = !fallbackToWeb && _state==ready && !_isWebSource` محسوبة قبل `setState`، تمنع فرض `_state=loading` عند إعادة اتصال (أو تبديل جودة/سيرفر يدوي) لجلسة native تعمل فعلاً — الفيديو يستمر ظاهراً (أو يختفي لحظياً بلا شاشة تحميل مقحمة) بدل ومضة كاملة | `c7b3686` |
| 36 | مؤكَّد بسجل فعلي (مسلسل بطيء الفتح): مرشّح HLS ميت فعلياً (اتصال CDN منقطع) كان يُهدر مهلة `initialize()` الكاملة (15 ثانية، من #33) قبل الانتقال للمرشّح التالي — مع أكثر من مرشّح بجلسة واحدة هذا يعني حتى 30 ثانية انتظار، رغم أن كل تشغيل ناجح فعلياً بالسجلات يكتمل خلال 3-5 ثوانٍ فقط | تخفيض المهلة من 15 لـ9 ثوانٍ — لا خطر إلغاء تشغيل بطيء لكن سليم (الفرق كبير عن أوقات النجاح الحقيقية المرصودة) | `c7b3686` |
| 37 | **حل جذري للتقطيع أثناء التشغيل الفعلي (وليس فقط بدء التشغيل)** — طُلب صراحة "حلول جذرية دفعة واحدة، مو تخمين": (أ) لا فرق بمعاملة `hls_cache_proxy.dart` بين مصدر منتهٍ (`#EXT-X-ENDLIST`، معروف الحجم بالكامل) وبث مفتوح فعلياً — كلاهما يُعامَل بنفس حذر "قد يختفي أي لحظة"، رغم أن أغلب/كل محتوى هذا التطبيق فعلياً فيديوهات مسجَّلة (تأكيد صريح من المستخدم). (ب) بعد إضافة تمييز VOD/live ورفع مقدار التخزين المسبق للمصادر المنتهية لـ12 شريحة، **سجل تشخيص فعلي تالٍ كشف انحداراً حقيقياً تسبَّبت به هذي الإضافة نفسها**: الـ12 شريحة كانت تُطلَق دفعة واحدة كـ12 اتصال HTTP متزامن لنفس خادم CDN (`unawaited` بلا أي حد) — عشرات `Connection closed while receiving data`/502/`Connection attempt cancelled` لعدة شرائح مختلفة بفارق أقل من 5 ميلي ثانية بينها، نمط كلاسيكي لخادم يحمي نفسه من اتصالات متزامنة كثيرة من نفس العميل (شائع بمواقع البث المقرصنة اللي هذا التطبيق يعتمد عليها) | (أ) `HlsCacheProxy._sourceHasKnownEnd` يُكتشَف من كل قائمة شرائح فعلية تُجلب (`#EXT-X-ENDLIST` موجود أم لا)، ويحدّد `_prefetchAhead` ديناميكياً: **12 شريحة** للمصادر المنتهية (لا خطر، الفيديو كامل ومعروف كيوتيوب)، **4** للمصادر المفتوحة فعلياً — يُسجَّل بـ`HLS_SOURCE_CLASSIFIED` لكل مصدر بالسجل التشخيصي للتحقق. (ب) طابور جلب مسبق (`_prefetchQueue`) بحد أقصى **3 اتصالات فعلية متزامنة فقط** (`_maxConcurrentPrefetch`) بغض النظر عن حجم التخزين المسبق المطلوب — الباقي ينتظر دوره ويبدأ فور تحرّر اتصال؛ الجلب اللحظي المباشر لطلب ExoPlayer الفعلي (`_handleSegment`) يبقى فورياً بلا أي انتظار بالطابور — لا يتأثر إطلاقاً. أيضاً أُضيف هامش أمان خلف حافة البث المباشر (شريحتان مخفيتان عن ExoPlayer، تُجلبان بالخلفية مسبقاً — مبدأ يوتيوب/تويتش) لمصادر لا تزال مفتوحة فعلياً فقط | `67b104e`, `a4f5261`, `7f53f5b` |
| 38 | مؤكَّد بسجل فعلي (فيلم يرفضه CDN بـ403 مباشرة): منقذ WebView (`_relayManifestViaWebView`، يجلب المانفست بجلسة WebView الحقيقية ويكتبه ملفاً محلياً `file://` عند فشل الرابط المباشر) كان مسار **بلا أي حماية إطلاقاً** — تعديل سابق بنفس هذي الجولة تخطّى `HlsCacheProxy` بالكامل لملفات `file://` (اعتقاداً أن `http.Client` لا يدعمها فمؤكَّد الفشل، وهذا صحيح بالطريقة القديمة). سجل تشخيص فعلي تالٍ أثبت أن هذا كان الخطأ لا الحل: تشغيل كامل بلا أي مؤشر `HLS_PROXY_*` واحد، عشرات دورات `NATIVE_BUFFERING_START/END` خلال أجزاء من الثانية، والفيديو يزحف من 2.7 إلى 3.5 ثانية فقط خلال 36 ثانية حقيقية — ExoPlayer يجلب كل شريحة بعيدة مباشرة بلا تخزين/إعادة محاولة | `HlsCacheProxy._handlePlaylist` يفرّع الآن حسب المخطط: `file://` يُقرأ من القرص مباشرة (`File.readAsString`)، بقية المخططات (http/https) تسلك نفس مسار `http.Client` القديم — القائمة المحلية بعدها تمرّ بنفس `_rewritePlaylist`/التخزين/إعادة المحاولة كأي مصدر HTTP عادي (روابط سيجمنتها مطلقة أصلاً، يكتبها `_relayManifestViaWebView` قبل الحفظ). **درس مهم**: لا تفترض "مؤكَّد الفشل فسنتخطّاه" لمسار حرج فعلياً (هذا تحديداً يُفعَّل كل مرة يرفض المصدر الجلب المباشر — أكثر الحالات حساسية) بدون التحقق من إمكانية إصلاح السبب الجذري نفسه أولاً | `71f7741` |
| 39 | مؤكَّد بسجل فعلي: مسلسل اشتغل بجلسة سابقة وتوقف فجأة بجلسة لاحقة بلا سبب ظاهر — `looksLikeHls()` (`candidate_scoring.dart`) تطابق أي رابط فيه `/hls/` كمقطع مسار، ومفتاح تشفير AES-128 (`encryption.key`) يعيش عادةً بنفس مسار `/hls/.../` الخاص بالقائمة والشرائح نفسها على أغلب خوادم CDN — فيُمنح نقاط تقييم عالية (100+) رغم إنه ملف مفتاح خام غير قابل للتشغيل إطلاقاً. التسلسل الفعلي بالسجل: بعد فشل كل مرشّحي الفيديو الحقيقيين واستنفاد ميزانية المحاولات (2/2)، إعادة فحص المصادر اللاحقة استبعدت المرشّحين الحقيقيين (already quarantined) لكن `encryption.key` لم يكن مجرَّباً من قبل فتصدّر كـ"دليل قوي" (`SOURCE_VALIDATION_SKIPPED reason=strong_evidence`) — أُرسل لمحاولة تشغيل أصلي (`PLAY_SERVER_QUALITY_START .../encryption.key`)، فشل حتماً (`ExoPlaybackException: Source error`)، ثم استُنفدت كل محاولات إعادة الاتصال التلقائي الثماني بإعادة نفس المحاولة الفاشلة حتماً تكراراً | `.key` أُضيفت لقائمة امتدادات `CandidateScoring.isNonMediaAsset` (نفس آلية تصفية `.jpg`/`.png`/`.css` الموجودة أصلاً) — يُستبعَد من الأساس بمرحلة الفلترة الأولى (`_isNonMediaAsset` بـ`watch_screen_discovery.dart`)، قبل حتى حساب أي نقاط تقييم له. اختبار جديد بـ`candidate_scoring_test.dart` يثبّت السلوك برابط حقيقي من السجل | `f730f60` |
| 40 | مؤكَّد بسجل فعلي منفصل لنفس المسلسل، **نفس فئة الخلل بالضبط اللي أُصلحت بـ#39 لكن لشريحة خام (`.ts`) بدل مفتاح تشفير**: `scoreDetectedSource` يخصم فعلاً 100 نقطة لأي رابط شريحة (`.ts`/segment) — لكن هذا الخصم كان يُلغى ببونص "مسجَّل كـhls بالسجل" (+85) أو "من إطار عمل معروف" (+85) لو ظهر نفس رابط الشريحة بمصدر ثانٍ (شائع فعلياً — JWPlayer يقرأ رابط الشريحة الحالية من قائمة تشغيله الداخلية أحياناً)، فيتجاوز حد الـ80 المطلوب رغم الخصم. نفس التسلسل: `NATIVE_TRIAL_QUEUED: url=.../seg-1-v1-a1.ts attempt=2/2` → فشل حتماً → 8 محاولات إعادة اتصال تلقائي كلها على نفس رابط الشريحة | نفس آلية #39: `.ts` مضافة أيضاً لقائمة امتدادات `isNonMediaAsset` — تُستبعَد من الأساس قبل أي حساب نقاط، فلا يقدر أي بونص يلغي الاستبعاد بعد الآن. اختبار جديد يثبّت هذا برابط حقيقي من السجل | `0b1d91e` |
| 41 | **طلب صريح: مراجعة هندسية شاملة سطر بسطر بحثاً عن أخطاء من نفس فئة #39/#40 وكود عديم الفائدة** — 4 نتائج مؤكَّدة بقراءة الكود مباشرة (لا سجل تشخيص هذي المرة، الأخطاء بنيوية وليست ظرفية): (أ) ثغرة حقيقية بنفس آلية حد الاتصالات المتزامنة (#37): مهام جلب مسبق مُطلَقة قبل `stop()` تبقى شغّالة بالخلفية، وعند اكتمالها بعد بدء جلسة تالية بنفس الكائن كانت تُفسد عدّاد/طابور الجلسة الجديدة — يعيد إنتاج نفس مشكلة قصف الاتصالات اللي الحد أُضيف أصلاً ليمنعها. (ب) مسار "استخراج رابط عام" منفصل تماماً (`_extractBestUrl`/`_fetchAndAnalyzePublicUrl` بـ`watch_screen.dart`) لم يكن يطبّق `isNonMediaAsset` إطلاقاً رغم كونه نفس فئة خطأ #39/#40 — رابط `.key` لا يُخصَم له أي نقاط بـ`scoreDetectedSource` أصلاً فيفوز بسهولة أكبر عبر هذا المسار تحديداً. (ج) `_handleSegment`: جسم فارغ (200 OK بلا بيانات، يحصل فعلياً من خوادم CDN متقلقلة) كان يرمي `ArgumentError` غير متوقَّع (`start.clamp(0,-1)`) بدل فشل جلب نظيف. (د) كود ميت: فحص `_prefetchQueue.contains(url)` بـ`_prefetchUrl` كان دائماً `true` كلما `_prefetching.contains(url)` صحيح أيضاً (يُضافان معاً، يُزالان معاً) | (أ) `_proxyGeneration` يزيد بكل `stop()`؛ كل مهمة جلب تحجز الجيل وقت إطلاقها وتتجاهل تحديث الحالة المشتركة لو تغيّر الجيل بحلول اكتمالها. (ب) `isNonMediaAsset` أُضيف بموضعين: حلقة الترشيح بـ`_extractBestUrl` (يسمح باختيار مرشّح آخر أفضل)، وكشبكة أمان نهائية عند قبول أي رابط بـ`_fetchAndAnalyzePublicUrl` (يغطي مسار الاستخراج العميق من JSON اللي لا يُرشِّح مرشّحيه إطلاقاً). (ج) `bytes == null \|\| bytes.isEmpty` بدل `bytes == null` فقط. (د) الفحص الميت حُذف | `9955186` |
| 42 | مؤكَّد بسجل فعلي (`sports_player_debug_log_4.txt`، مسلسل يعلَّق على شاشة التحميل ولا يتحرك أبداً): `HlsCacheProxy._fetchSegmentWithRetry` كان يلتقط `_client` مرة واحدة فقط بأول السطر ويعيد استخدام نفس الكائن بمحاولاته الثلاث لإعادة الجلب. المشغّل يجرّب أكثر من رابط مرشّح خلال نفس الجلسة (مثلاً `master.m3u8` ثم `index-v1-a1.m3u8` بعد فشل الأول) وكل تبديل يستدعي `start()` الذي يستدعي `stop()` أولاً — و`stop()` يُغلق `_client` الحالي. أي طلب شريحة كان قيد إعادة محاولة فعلاً لحظة هذا الإغلاق يبقى ممسكاً بنفس كائن `client` القديم المُغلَق، فمحاولاته المتبقية تفشل حتماً بخطأ `Client is already closed` — لا علاقة له بالشبكة إطلاقاً. السجل أظهر هذا النمط بالضبط 3 مرات متتالية (محاولة 0: خطأ اتصال حقيقي عابر من CDN، محاولتا 1 و2: `already closed`) مع كل بورت وكيل جديد، فتضيع كل فرصة نجاح فعلية لأي شريحة ويُعلَّق المشغّل بلا نهاية. نفس فئة الخلل كانت موجودة أيضاً بحلقة إعادة محاولة جلب القائمة (`_handlePlaylist`) | كلا الموقعين الآن يحجزان `_proxyGeneration` مرة عند البداية، ويعيدان قراءة `_client` طازجاً + التحقق من تطابق الجيل **قبل كل محاولة** بدل الالتقاط مرة واحدة — لو تغيّر الجيل (تبديل مرشّح/جلسة جديدة) تتوقف المحاولة فوراً بدل تضييع وقت على فشل مؤكَّد، وهذا فعلياً يُسرّع تحرّر فتحات الجلب المتوازي (`_maxConcurrentPrefetch`) لصالح الجلسة الجديدة بدل حجزها بمحاولات ميتة — لا تغيير على حد التزامن نفسه ولا على سرعة الالتقاط المتوازي للمرشحين | (كوميت لاحق) |
| — | **تأكيد**: `debug_log_1.txt` (مسلسل اشتغل بمشغّل الويب تلقائياً لكن لا بالمشغّل الأصلي) أظهر نفس نمط `Client is already closed` بالضبط لإصلاح #42 — لكن تبيّن أن البناء المُختبَر لم يكن يحوي الإصلاح بعد (لا تشخيص خاطئ، البناء نفسه قديم). **درس لجلسات مستقبلية**: قبل تشخيص أي فشل proxy من جديد، تأكد أولاً أن `git log --oneline -1` بجهاز المستخدم يطابق أو يتقدّم على آخر كوميت مسجَّل بهذا الجدول — التشخيص من سجل ببناء قديم يهدر وقتاً على مشكلة مُصلَحة فعلياً | — | — |
| 43 | **طلب صريح: هندسة معمارية — لكل نوع مشغّل ويب معروف (video.js، JWPlayer/vidmoly) طريقة تقييم/استبعاد مرشّحين معزولة فيزيائياً، بحيث إصلاح خاص بنوع لا يقدر يؤثر على نوع آخر بالخطأ**. المراجعة أظهرت أن اكتشاف *نوع* المشغّل نفسه كان سريعاً وصحيحاً أصلاً (جافاسكربت مُحقَن + مطابقة دومين، أقل من نصف ثانية بكل السجلات)، لكن *بعد* الاكتشاف كل الأنواع كانت تمرّ بنفس دالتي `scoreDetectedSource`/`isNonMediaAsset` المشتركتين بـ`candidate_scoring.dart` — وهذا بالضبط جذر أخطاء #39 (مفتاح تشفير JWPlayer)، #40 (شريحة .ts JWPlayer)، #41ب (مسار استخراج عام لم يطبّق الاستبعاد إطلاقاً) الثلاثة | طبقة استراتيجيات جديدة (`lib/services/player_strategies/player_strategy.dart`): `PlayerStrategy` مجرَّدة (القاعدة = نفس `CandidateScoring` القديم بلا أي تغيير سلوك)، وتحتها `GenericPlayerStrategy`/`VideoJsPlayerStrategy`/`JwPlayerStrategy` — كل واحدة تقدر تُضيف/تُعدِّل نقاط تقييمها الخاصة بلا لمس البقية. `PlayerStrategyRegistry.select()` يختار الاستراتيجية حسب أعلام الاكتشاف الموجودة أصلاً (`_webVideoJsPlayerMode`/`_webVidmolyPlayerMode`، الأخير يغطي JWPlayer أيضاً — راجع تعليقها). وُصلت بحلقة التقييم الوحيدة بـ`watch_screen_discovery.dart` (استبدال الصيغة المضمَّنة `+85/+85` بـ`strategy.scoreCandidate(...)`) — **القيم الافتراضية مطابقة تماماً للصيغة القديمة، صفر تغيير سلوك بهذا الكوميت وحده**؛ الفائدة الفعلية تظهر بإصلاحات JWPlayer/video.js *المستقبلية* اللي تُكتب بملفها الخاص من الآن. اختبارات جديدة (`test/player_strategy_test.dart`) تثبّت تطابق السلوك القديم + تثبّت العزل فعلياً (تعديل استراتيجية اختبارية لا يمس أي استراتيجية أخرى) | (كوميت لاحق) |

| 44 | مؤكَّد بسجل فعلي (`sports_player_debug_log_2.txt`، مسلسل يبقى على شاشة المشغّل بلا حراك — لا تجمّد تطبيق، بل فيديو متجمّد فعلياً): **تأكيد أن إصلاح #42 يعمل فعلاً** (صفر أخطاء `already closed` بكامل السجل، مقارنة بسجلات سابقة). لكن السجل كشف خللين حقيقيين إضافيين بمسار الرجوع لمشغّل الويب (WebView fallback): (أ) `_revealVidmolyPlayer`/`_revealVideoJsPlayer` لم تكونا تضبطان `_webPlaybackReady = true` بعد الكشف (خلاف `_showWebPlaybackReady` المشابهة لها اللي تضبطه فعلاً) — حارسهما الخاص (`\|\| _webPlaybackReady`) كان ميتاً دائماً، فتُعاد استدعاؤهما بكل دورة اكتشاف (~كل ثانيتين) بدل مرة واحدة، وتترك حراس أخرى بالكود (`_webPlaybackReady` تُستخدَم بـ5 مواضع أخرى لمنع استكشاف/إعادة تحميل غير ضروري بعد الكشف) بلا أثر طوال الجلسة. (ب) **السبب الأرجح للتجمّد نفسه**: `_webPlaybackSentinel` (الدالة الوحيدة اللي تتحقق من تشغيل حقيقي فعلاً — `playing`/`currentTime` يتقدّم) لم تُثبِت تشغيلاً حقيقياً ولا مرة واحدة خلال 63 ثانية كاملة (صفر `WEB_PLAYBACK_EVIDENCE` بالسجل)، رغم إن `FALLBACK_TO_WEBVIEW ... proof=true` تكرّر عشرات المرات — يعني "الدليل" كان دائماً من المسار الاحتياطي الشبكي البحت (عدد موارد الشبكة يتزايد) لا من ملاحظة فعلية لعنصر `<video>` يشتغل، وهذا يسمح بإعلان الصفحة "جاهزة" حتى لو كان `<video>` نفسه غير مرئي أصلاً لجافاسكربت المُحقَن (مرشّح قوي: داخل iframe من أصل مختلف) | (أ) أُضيف `_webPlaybackReady = true` بكلا الدالتين، بنفس نمط `_showWebPlaybackReady`. (ب) تسجيل تشخيصي جديد (`WEB_PLAYBACK_PROOF_NETWORK_ONLY`) بكل مرة يُثبَت فيها "دليل" عبر المسار الشبكي البحت فقط — يحمل `found`/`playing` من آخر لقطة حقيقية للعنصر، ليؤكد بسجل تالٍ هل العنصر غير مرئي أصلاً (`found=false`) أم مرئي لكن متوقف (`found=true playing=false`) — **يحتاج سجل جديد لنفس المشكلة للتأكيد النهائي، لم يُصلَح جذر التجمّد نفسه بعد** | (كوميت لاحق) |

| 45 | مؤكَّد بسجل فعلي (موقع جديد أُضيف `4b.1i2cqoi.shop`، مسلسل ون بيس — المستخدم أبلغ أنه يقدر يبدّل بين سيرفرات المشاهدة بالمتصف العادي لكن ليس داخل التطبيق): فحص "هل يوجد دليل تشغيل شبكي" (`mediaResourceLike` بجافاسكربت المُحقَن + نظيره بـ`_webPlaybackSnapshot`) كان له إيجابيتان كاذبتان حقيقيتان ظهرتا بالسجل مباشرة: (أ) رابط أيقونة **favicon** من جوجل (`s2.googleusercontent.com/s2/favicons?domain_url=...`) يحمل رابط صفحة الحلقة نفسها بمعامل الاستعلام — يحتوي غالباً كلمات "video"/"watch" فيُطابق فحص الكلمات المفتاحية رغم كونه مجرد طلب أيقونة 16×16. (ب) رابط إعلان كازينو حقيقي (`amd-cdn-*.../content/**stream**/agl/1xbet_city_*.gif`) يحتوي كلمة "stream" بمساره التسويقي الداخلي (تسمية إعلانية لا علاقة لها بالبث). النتيجة الفعلية بالسجل: `_webMediaEvidenceScore` وصل 100 خلال أقل من 10 ثوانٍ من تحميل أيقونات/إعلانات فقط، **قبل** أي محاولة اكتشاف مشغّل حقيقي — وهذا "الدليل" الكاذب أسكت شرط النقر التلقائي (`!await _webPlaybackSentinel(...)` بحلقة الاكتشاف) طوال الجلسة كاملة (صفر `AUTO_CLICK_ATTEMPT` بكل السجل رغم وصوله لحد ميزانية المحاولات 16)، فلا يُنقَر أي زر سيرفر تلقائياً إطلاقاً على مواقع بهذا الشكل، ولا تُكتشَف أي قائمة تشغيل حقيقية طوال الجلسة (`SOURCES_SCAN` بقي `generic=2 registry=0` من البداية للنهاية) | كلا الفحصين (`mediaResourceLike` بالحقن الرئيسي، والنسخة المكررة بـ`_webPlaybackSnapshot`) عُدِّلا معاً: (أ) فحص الكلمات المفتاحية يتجاهل معامل الاستعلام كلياً الآن (نطاق+مسار فقط) — رابط favicon يحمل الصفحة كاملة بمعامل استعلام لا يعود يُطابَق. (ب) أي مورد `initiatorType` الحقيقي صورة/ستايل (`img`/`css`/`link`) أو امتداده صورة/خط معروف (`.gif`/`.png`/`.ico`/...) يُستبعَد قبل أي فحص كلمات مفتاحية إطلاقاً — شريحة/قائمة تشغيل حقيقية لا تُحمَّل أبداً بهذي الأنواع. **درس عام**: أي فحص "دليل شبكي" مبني على كلمات مفتاحية عامة (`stream`/`video`/`media`) بلا استبعاد نوع المورد الحقيقي معرَّض لإيجابيات كاذبة من إعلانات/تتبّع حقيقية بمواقع البث المقرصنة تحديداً (غنية بالإعلانات بطبيعتها) | (كوميت لاحق) |
| 46 | طلب المستخدم: إعلان مقلَّد بشكل كابتشا عربية ("انتباه! يرجى التأكد من أنك لست روبوتاً") ظهر فوق مشغّل فلم — **بدون سجل يوثّق الحادثة تحديداً** (بُحث بكل الكلمات المحتملة بـ4 سجلات مرفوعة، صفر نتائج)، فالتشخيص هذا مبني على قراءة كود آلية الحماية الموجودة أصلاً (`_installWebProtection`) لا على سجل. لُقيت ثغرتان حقيقيتان بهذي الآلية: (أ) `inspectIframes()` كان يتجاهل iframe إعلاني معروف (يطابق قائمة نطاقات إعلانية موجودة أصلاً) من كونه "مرشّح مشغّل" فقط، بلا أي تحييد فعلي — يبقى ظاهراً تفاعلياً بالكامل. (ب) `sensitivePrompt()` (قائمة الإخفاء) ما فيها أي كلمة عربية لنمط "أنت لست روبوت" رغم إن `humanChallenge()` (قائمة **الاستثناء** لكابتشا حقيقي) عندها مرادفها الإنجليزي — إعلان عربي بهذا النص ما يتطابق مع أي من القائمتين فيمر بحرية. دليل داعم غير مباشر: محتوى الفيديو الحقيقي كان يشتغل بنجاح (segments حقيقية + JWPLAYER score=100) **قبل** ظهور أي شي بنفس الجلسة المُبلَّغة — يرجّح إعلان طعم لا حماية حقيقية من الموقع، فآمن إخفاؤه | (أ) iframe المطابق للقائمة الإعلانية يُخفى فعلياً (`display:none` + `pointer-events:none`) بدل تجاهله فقط. (ب) كلمات عربية (`روبوت`, `لست إنسان/انسان`, `التحقق الأمني`) أُضيفت لـ`sensitivePrompt()` فقط — `humanChallenge()` لم يُمس، فأي كابتشا حقيقي (Cloudflare/hCaptcha/reCAPTCHA) يبقى ظاهراً وقابلاً للحل كما هو. (ج) طبقة ديناميكية ثالثة بلا أي كلمة مفتاحية: `hideUnsafePrompts()` كانت تفحص حجم/موضع العنصر (`adLike`) فقط على عناصر `form/button/a/[class*=popup]/[class*=advert]` — أي `<div>` عادي بكلاس غير مطابق (نمط شائع لتفادي فلاتر بالضبط) لم يكن يوصله الفحص أصلاً. أُضيف مسح لعناصر `<body>` المباشرة بنفس فحص الحجم/الموضع بغض النظر عن اسم الكلاس/اللغة — مع فحص مزدوج (نفسه + محتواه) يستثني أي عنصر يحوي مشغّل فيديو حقيقي كسليل (تصميم مواقع فيديو شائع: غلاف كامل الشاشة يحتوي المشغّل الفعلي بداخله) لمنع إخفاء محتوى حقيقي بالخطأ. **لم يُختبَر بسجل فعلي بعد لنفس هذا الحادث تحديداً** — يحتاج تأكيد لاحق | (كوميت لاحق) |
| 47 | مواصلة تشخيص #45/محادثة سابقة (أنمي/أفلام تبقى على مشغّل الويب — `NATIVE_TRIAL_FAILED` بعد 9 ثوانٍ بالضبط، `TimeoutException: native initialize() timed out after 9s`، مع `HLS_PROXY_SEGMENT_FETCH_ERROR: Connection closed...` بنفس اللحظة): **إعادة قراءة الكود كشفت أن التشخيص السابق كان مؤطَّراً خطأ** — لما `hls_cache_proxy` يعمل بنجاح (يحصل بكل السجلات)، ExoPlayer **لا يتصل بالـCDN مباشرة إطلاقاً**، يتصل بخادمنا المحلي (`127.0.0.1`) فقط. يعني "فشل ExoPlayer" هو فعلياً **فشل عميل Dart HTTP الخاص بوكيلنا بجلب الشريحة من الـCDN** — مشكلة مختلفة تماماً عن "طبقة اتصال ExoPlayer الأصلية محجوبة تحديداً" كما افتُرض سابقاً. نقد كود إضافي كشف علّة حقيقية: `_headersForCandidate` (`watch_screen_discovery.dart`) يحسب صحيح إن كان مرشّح CDN مختلف الأصل عن صفحة التضمين (يضبط `origin`/`referer` بشكل صحيح عندها) لكنه **لا يصحّح `sec-fetch-site` المضبوط ثابتاً `'same-origin'` بـ`_headers` الأساسي (`watch_screen.dart`)** — طلب حقيقي لمصدر مختلف الأصل تماماً (نطاق CDN منفصل عن صفحة الفيديو) يرسل `sec-fetch-site: same-origin` بشكل يتناقض منطقياً مع `origin`/`referer` المرسَلين بنفس الطلب، وهذا نمط كشف بوتات معروف (فحص تطابق Sec-Fetch-Site مع Origin الفعلي) — **لم يُؤكَّد بسجل بعد كسبب فعلي**، لكنه خطأ منطقي حقيقي بغض النظر | (أ) تصحيح `sec-fetch-site` إلى `'cross-site'` فقط عندما مرشّح الـCDN فعلاً مختلف الأصل عن صفحة التضمين (نفس فرع `!sameOrigin` الموجود أصلاً). (ب) تشخيص إضافي بلا أي تغيير سلوك/توقيت/تزامن بـ`hls_cache_proxy.dart`: تسجيل أسماء الهيدرز المُرسَلة (لا قيمها) مرة واحدة عند `start()`، وزمن كل محاولة جلب قائمة/شريحة فاشلة (`elapsedMs`) — فشل فوري (<500ms) يرجّح رفضاً نشطاً من الـCDN لبصمة/توقيع عميلنا تحديداً، بينما اقتراب من حد المهلة يرجّح تعليق شبكة فعلي. **يحتاج سجل جديد لنفس مشكلة الأنمي للتأكيد** — لا يزال احتمال أن السبب الحقيقي شي آخر تماماً (توقيع الرابط نفسه مرتبط بجلسة/IP الطلب الأول، لا الهيدرز) | (كوميت لاحق) |
| 48 | مؤكَّد بسجل فعلي جديد (أنمي vampire-dormitory، `ristoanime.me`→`vidmoly`→`vmnow.online`): المستخدم أبلغ "شفت مشغل الويب شغّال ومشغلي ما التقطه إلا بعد فترة طويلة" — السجل يثبت هذا **بالضبط**: `PLAY_SERVER_QUALITY_SUCCESS` تحقق فعلاً (`final state: NATIVE`)، لكن بعد **6.9 ثانية** من `PLAY_SERVER_QUALITY_START` — **بصفر** أحداث `HLS_PROXY_SEGMENT_FETCH_ERROR` بكامل السجل (عكس السجلات الأربعة بالمناقشة السابقة اللي فيها فشل فعلي متكرر). يعني هذا مسار **سليم تماماً**، فقط أبطأ من افتراض ضبط مهلة `initialize()` بـ9 ثوانٍ (المبني وقتها على افتراض "أي نجاح فعلي يكتمل خلال 3-5 ثوانٍ") — بهامش أمان ~2 ثانية فقط قبل ما يُقتَل بالحد القديم رغم كونه اتصالاً سليماً يتقدّم فعلياً. **هذا يكشف أن مشكلة "الأنمي ما ينتقل للـnative" على الأقل جزئياً فرضيتان منفصلتان لا فرضية واحدة**: (أ) رفض CDN فعلي (سجل #47، فيه `HLS_PROXY_SEGMENT_FETCH_ERROR`)، (ب) هامش مهلة ضيق جداً لاتصال سليم لكن أبطأ من المعتاد (هذا السجل، صفر أخطاء اتصال) | مهلة `initialize()` رُفعت من 9 إلى 13 ثانية — هامش ~6 ثوانٍ فوق أبطأ نجاح حقيقي مسجَّل الآن (6.9s)، بدل العودة لـ15 كاملة (تفادياً لمشكلة "30 ثانية انتظار بمرشّحين ميّتين فعلياً" الأصلية اللي بررت التخفيض لـ9 أصلاً — راجع السجل الأصلي لهذا القرار أعلاه بنفس الملف). تشخيص إضافي: تتبّع `_lastWebMediaResourceHitAt` جديد + معامل `isWebViewActiveNearby` بـ`HlsCacheProxy` يُضاف الآن لكل سطر `HLS_PROXY_SEGMENT_FETCH_ERROR/HTTP_ERROR` — يثبت مباشرة بالسجل القادم هل WebView كان نشطاً شبكياً بنفس لحظة فشل جلبنا (فرضية ضغط اتصالات متزامنة من نفس IP، مطروحة بنفس المحادثة، لم تُختبَر بعد) | (كوميت لاحق) |
| 49 | **تأكيد فرضية #47/#48 بالتشخيص الجديد نفسه**: سجل فعلي لاحق (topcinema/reacher، `serv-stream-cdn28.cdn-video.xyz`) أظهر `HLS_PROXY_SEGMENT_FETCH_ERROR` مع `webViewActiveNearby=true` **و**`elapsedMs` كبيرة جداً (11298، 12797، 4779ms — لا فشل فوري إطلاقاً). هذا ينقض فرضية "رفض فوري لبصمة الطلب" (إصلاح Sec-Fetch-Site #47 وحده لم يكفِ رغم وجوده صحيحاً بالهيدرز المسجَّلة) ويرجّح بقوة **تعليق/تأخير من الـCDN بسبب اتصال WebView النشط المتزامن فعلاً بنفس اللحظة** — لم يُنفَّذ حل لهذا بعد (يحتاج قرار معماري، الخيار الوحيد بلا مخاطرة تجميد لم يُحسَم). **مشكلة منفصلة تماماً بنفس البلاغ**: إعلان مقلَّد بشكل نافذة نظام ("انتباه" + "انقر للمزيد للمتابعة" + زري "أكثر"/"إغلاق" — سكرين شوت فعلي من المستخدم) ظهر فوق مشغّل vidtube/JWPlayer، **بدون أي أثر بالسجل النصي** (طبيعي — تراكب DOM بحت، لا حدث شبكة/تنقّل). السبب الجذري المؤكَّد من قراءة الكود مع السكرين شوت: `hideUnsafePrompts()` كانت تستثني **كل** عنصر داخل حاوية المشغّل (`playerLike()`/`.jwplayer`/`.jw-wrapper`) **قبل حتى فحص كلمات `sensitivePrompt`** — إعلان مُحقَن داخل نفس حاوية المشغّل (نمط شائع) كان محمياً بالكامل من الإخفاء بهذا التصميم، رغم كونه غير حقيقي 100% (نص "انتباه"/"انقر للمزيد للمتابعة" غير موجود بأي زر تحكّم حقيقي) | إعلان النافذة المقلَّدة: (أ) كلمات عربية دقيقة (`انتباه`, `انقر للمزيد`, `للمتابعة`) أُضيفت لـ`sensitivePrompt()`. (ب) **الإصلاح الجوهري**: أعيد ترتيب `hideUnsafePrompts()` — فحص `sensitivePrompt` (كلمة مفتاحية دقيقة، خطر إيجابية كاذبة منخفض جداً) يُنفَّذ الآن **قبل** استثناء `playerLike()` ويبحث أيضاً بأقرب حاوية تشبه نافذة/مربع حوار (`closest('[role="dialog"],[class*="modal"],[class*="dialog"],[class*="alert"]')`) ليخفي الإعلان كاملاً لا الزر وحده — فيعمل حتى لو الإعلان داخل حاوية المشغّل. فحص `adLike()` العام (حجم/موضع فقط، بلا كلمة مفتاحية، خطر إيجابية كاذبة أعلى — مثل قائمة جودة حقيقية) **يبقى مستثنى من داخل المشغّل كما كان**، لم يُمس. مشكلة ضغط الاتصالات: لم تُحَل بعد، تحتاج نقاش/قرار | (كوميت لاحق) |

| 50 | **السبب الجذري الحقيقي لمشكلة "المشغّل لا يلتقط المصدر"** — مؤكَّد بتحليل 7 سجلات فعلية معاً: كل فشل جلب شريحة بالوكيل (`HLS_PROXY_SEGMENT_FETCH_ERROR`) كان على الجودة الأعلى (`_x`) أو العالية (`_h`)، **ولا فشل واحد** على `_n`/`_l`. والمصدران الوحيدان اللذان نجحا نظيفَين (`d4f65fa0`, `f806b29d`) هما اللذان لا يملكان جودة ثقيلة أصلاً (`,n,l,` فقط). بنفس اللحظة كان مشغّل الموقع داخل WebView يشغّل `_n`/`_l` بنجاح — أي أن آلية التكيّف الخاصة بالموقع قرّرت أن الشبكة لا تتحمّل الأعلى. والكود كان يطلبها عمداً: `variants.sort((a,b) => b.$1.compareTo(a.$1))` بـ`watch_screen_discovery` (تنازلي، "الأعلى أولاً") ثم `qualities.first` بـ`watch_screen:1420`. فبعد فشل المحاولة الأولى **يعيد المحاولة بنفس الجودة التي فشلت للتو**. النتيجة: شريحة بعدة ميغابايت تزحف 11-13 ثانية (`elapsedMs=11298`/`12797`) ثم `Connection closed while receiving data`. **هذا يُبطل فرضيات #47** (بصمة بوت/Sec-Fetch-Site/حجب CDN): `webViewActiveNearby=true` عامل مُفاقِم (WebView يستهلك النطاق بالتوازي) لا سبب. ورُصد أيضاً فشل بمنتصف تشغيل ناجح (`_x/seg-83`, `_x/seg-3`) = تقطيع بعد صعود ABR للأعلى | وحدة معزولة مختبَرة `services/hls_variant_selector.dart` (16 اختبار): (أ) تُطابق الجودة بـ**مجلد الرابط** لا بنمط تسمية خاص بمزوّد — تعمل مع أي CDN. (ب) نتتبّع الجودة التي أثبت WebView جلب شرائحها فعلاً (`WEB_PROVEN_VARIANT`) ونقدّمها للتشغيل الأصلي. (ج) بلا إثبات: نؤخّر الأعلى للآخر **فقط** حين تكون الجودات 3 فأكثر (مصادر الجودتين نجحت بالأعلى فعلاً — لا تخفيض بلا سبب). (د) سقف تكيّف بالوكيل (`_applyVariantCeiling`) يحصر قائمة الجودات المسلَّمة لـExoPlayer فلا يبدأ بالأثقل ولا يصعد إليها بالمنتصف — معطَّل تماماً حين لا إثبات ولا ما يُحذف (سلوك قديم حرفياً). لا تُحذف أي جودة من الاختيار اليدوي | `818f172` |
| 51 | إعلانات التراكب المزيّفة (نافذة "انتباه/انقر للمزيد للمتابعة" بزري أكثر/إغلاق فوق المشغّل، وقبلها نسخة "روبوت"): الدفاع كان قوائم كلمات مفتاحية وأسماء كلاسات — يُهزَم بتغيير نص واحد، وأسماء الكلاسات تُولَّد عشوائياً أصلاً لتفادي الفلاتر. كذلك تراكب داخل iframe من أصل مختلف لا يصله جافاسكربتنا إطلاقاً (قيد متصفح لا يُتجاوز) | ثلاث طبقات مستقلة: (أ) **كاسح بنيوي لا لغوي** (`kOverlayAdSweeperScript`) لا يقرأ أي كلمة ولا يعتمد أي اسم — يحكم بالهندسة والسلوك: تغطية منطقة الفيديو + طبقة عالية + ظهور بعد بدء التشغيل + قصر النص وقلة الأزرار. **بوابة إلزامية** تمنع حجب واجهة حقيقية بالخطأ: إما رابط/زر لنطاق مختلف أو فتح نافذة (هدف الإعلان الوحيد)، أو أن يكون خارج حاوية المشغّل رغم تغطيته الفيديو — فقائمة جودة حقيقية (تظهر بعد التشغيل وبالمنتصف، لكن داخل المشغّل وبلا رابط خارجي) لا تُمس. (ب) `MutationObserver` يسجّل زمن ميلاد كل عنصر مُضاف، فـ"ظهر بعد بدء التشغيل" تصبح إشارة قابلة للقياس. (ج) **زر نجاة بطبقة Flutter** فوق الـWebView (خارج متناول الصفحة تماماً): يحذف فورياً أي تراكب يغطي **مركز** الفيديو — النوافذ المزيّفة تتوسّط الصورة دائماً بينما شريط التحكم يلتصق بالحافة فلا يُحذف. تسجيل `OVERLAY_AD_BLOCKED`/`OVERLAY_AD_MANUAL_SWEEP` يثبت بالسجل ما حُجب فعلاً | `542956e`+ |
| 52 | `watch_screen_discovery.dart` بلغ 3081 سطراً، **ثلثها تقريباً** نصوص خام لجافاسكربت (كتلة واحدة 547 سطراً) — يخنق قراءة منطق Dart ومراجعته، ويجعل أي تعديل بالمنطق محفوفاً بخطر المساس بنص JS بالخطأ | نُقلت 8 كتل (930 سطراً) إلى `screens/web_injection_scripts.dart` كثوابت مسمّاة وموثّقة. النقل **برمجي** ومُتحقَّق منه بمقارنة حرفية للكتل الثمانية قبل/بعد + مطابقة توازن الأقواس عبر الملفين للأصل. صفر تغيير سلوك. النتيجة: 3081 → 2163 سطر (-30%) | `542956e` |

**ملاحظة إضافية لجلسات مستقبلية**: لو ظهر "كان يعمل وتوقف فجأة بلا سبب واضح" لمصدر HLS، تحقق دائماً من قيمة `url=` بأحداث `NATIVE_TRIAL_QUEUED`/`PLAY_SERVER_QUALITY_START`/`NATIVE_AUTO_RECONNECT` بالسجل — لو كانت تشير لملف غير `.m3u8` حقيقي (مثل `.key`/`.ts`/أي امتداد آخر غير قائمة تشغيل)، السبب مؤكَّد اختيار مرشّح خاطئ بمرحلة الاكتشاف (`_scoreDetectedSource`/`_isNonMediaAsset`) — **نمط متكرر**: أي بونص إضافي بحساب النقاط (`registered?.type == 'hls'`, `frameworkSources.contains`) يقدر يُلغي خصم `scoreDetectedSource` السلبي لملف غير صالح، فالاستبعاد الوحيد الموثوق هو `isNonMediaAsset` (بمرحلة الفلترة الأولى، قبل أي بونص) — لا الاعتماد على النقاط النهائية وحدها.

## ملاحظات مهمة لجلسات مستقبلية (اقرأ قبل تعديل `hls_cache_proxy.dart`/منطق إعادة الاتصال)

- **لا تُعِد رفع `_prefetchAheadVod`/`_prefetchAhead` بدون رفع `_maxConcurrentPrefetch` معه بنفس النسبة تقريباً** — الدرس المباشر من #37: زيادة عدد الشرائح المطلوب تخزينها مسبقاً بلا حد تزامن يقصف خادم CDN باتصالات متزامنة كثيرة، وأغلب مواقع البث المقرصنة تحمي نفسها من هذا بقطع الاتصال — **يزيد التقطيع بدل ما يقلّله**، وهذا فعلياً ما حصل.
- **لا تحذف/تتخطَّ فرعاً كاملاً بافتراض "مؤكَّد الفشل" (مثل `file://` بـ#38) دون التحقق أولاً إن كان بالإمكان إصلاح السبب الجذري** (هنا: `http.Client` لا يدعم `file://`، لكن قراءة الملف مباشرة بديل بسيط كان متاحاً طوال الوقت) — تخطّي المسار بالكامل يفقد أي حماية (تخزين/إعادة محاولة) كان يستفيد منها ذلك المسار تحديداً، وغالباً هو المسار الأكثر حاجة للحماية (يُفعَّل فقط عند فشل المسار الأول).
- **جميع إصلاحات #35-38 مدفوعة فعلياً لفرع `claude/greeting-a1f5q5`** بالكوميتات المذكورة أعلاه، بعد اختبار فعلي على جهاز حقيقي (سجلات تشخيص متعددة قبل وبعد كل تعديل) — لا تُعِد تشخيصها من الصفر أو تراجع عنها بدون سجل تشخيص جديد يثبت مشكلة فعلية.
