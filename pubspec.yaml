name: sports_stream_app
description: تطبيق بث رياضي احترافي مع لوحة تحكم ديناميكية عبر Firebase
publish_to: 'none'
version: 1.0.2+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^2.32.0
  cloud_firestore: ^4.15.5
  url_launcher: ^6.3.1
  android_intent_plus: ^5.2.0
  shared_preferences: ^2.3.2
  share_plus: ^10.1.2
  http: ^1.2.2
  video_player: ^2.9.2
  chewie: ^1.8.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
